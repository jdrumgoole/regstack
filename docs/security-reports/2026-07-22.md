# Security Review — 2026-07-22

**Reviewer:** Automated security agent (scheduled daily review)
**Version reviewed:** 0.8.6 (`src/regstack/version.py`)
**Scope:** Full 15-section review per `scripts/security-review-prompt.md`
**Previous review:** 2026-07-21 (0 warnings, 9 INFO, 0 critical — `[security-clean]`)

> **Since the last review (2026-07-21):** One commit has landed on `main`:
>
> - `b35b2a3 [security-clean] Security Review — 2026-07-21` — docs-only, the
>   daily security report. No product code changes.
>
> **Net change in findings:** CVE search for the July 21–22 window found no new
> advisories for any tracked package. No code changes; no findings opened or
> closed. This is the third consecutive `[security-clean]` review.

---

## 🔴 CRITICAL (Immediate action required)

None.

---

## 🟠 WARNING (Address within 1 week)

None.

---

## 🟡 INFO (Best practice recommendations)

All nine carry-forward items from 2026-07-21 remain open, unchanged. No new INFO items.

### I-1 (carry-forward) — `UserPublic` exposes `tokens_invalidated_after`

`UserPublic` includes `tokens_invalidated_after: datetime | None`
(`src/regstack/models/user.py:105`). This is an intentional design choice
(SPAs compare it to the session JWT `iat` to detect bulk-revocation without
an extra round-trip). The exposure leaks the exact timestamp of the most
recent security event (password change, email change, admin-disable) to any
authenticated client. Acceptable for regstack defaults; hosts with stricter
data-minimisation requirements should subclass `UserPublic` or project out
the field. Recommend documenting the trade-off explicitly in `docs/security.md`.

### I-2 (carry-forward) — `admin_promote_pending` path parameter not validated as `EmailStr`

`POST /admin/pending/{email}/promote` (`src/regstack/routers/admin.py:204`)
accepts `email: str = Path(...)`. A syntactically invalid email address passes
parameter binding and returns 404. The endpoint is admin-gated so risk is low.
One-line fix: `email: Annotated[EmailStr, Path(...)]`.

### I-5 (carry-forward) — Email-change token JTI not blacklisted on first use

**File:** `src/regstack/routers/account.py:175` (`confirm_email_change`)

`POST /confirm-email-change` does not blacklist the token's `jti` after
successfully completing the email swap. The password-reset handler does
blacklist its token's `jti` at `src/regstack/routers/password.py:117`. The
email-change token has a 1-hour TTL (default). An attacker who captures the
URL from browser history, proxy logs, or email forwarding can replay it within
that window; `update_email()` accepts the second call silently (same user,
same email — no constraint violation) and bumps `tokens_invalidated_after`,
forcing the user to re-authenticate.

The practical impact is low: no email hijacking is possible (the target address
is already set), but the attacker can cause repeated forced re-logins until
token expiry. The fix is a one-liner matching the reset-password pattern:
call `await rs.blacklist.revoke(jti, exp)` after `update_email()` succeeds.

### I-6 (carry-forward) — "X attempt(s) remaining" count in 401 response body

**File:** `src/regstack/routers/login.py:42`

`_build_invalid()` includes the remaining failure count in the 401 detail
message: `f"Invalid email or password. {remaining} attempt(s) remaining."`.
This tells a credential-stuffing attacker exactly when to pause to avoid
lockout, enabling systematic pacing of brute-force attempts to stay under
the threshold indefinitely. Recommendation: return a static
`"Invalid email or password."` message regardless of remaining attempts.

### I-7 (carry-forward) — `jwt_audience` defaults to `None`; no multi-instance deployment guidance

**File:** `src/regstack/config/schema.py:189`

With `jwt_audience = None`, pyjwt skips audience validation on decode. If two
regstack instances share the same `jwt_secret` (a microservice topology where
one JWT secret is shared across services), a session token issued by instance A
would be accepted by instance B. No audience guidance appears in
`docs/configuration.md` or the examples. Recommendation: add a configuration
note advising hosts to set `jwt_audience` to a service-unique string in any
deployment where multiple regstack instances share a `jwt_secret`.

### I-8 (carry-forward) — No HTTP security headers set or demonstrated

**Scope:** SSR pages served by `ui_router`

regstack does not set `Content-Security-Policy`, `X-Frame-Options`,
`X-Content-Type-Options`, or `Referrer-Policy` headers. This is expected for
an embedded library — host middleware owns the response headers. However,
neither the `examples/` demos nor `docs/embedding.md` show a recommended
middleware stack for hosts that enable `ui_router`. Recommendation: add a
"Security hardening" section to the embedding guide showing a simple
security-headers middleware configured for the regstack SSR prefix.

### I-9 (carry-forward) — `twilio_account_sid` typed as `str | None` rather than `SecretStr`

**File:** `src/regstack/config/schema.py:98`

Every other credential field in `RegStackConfig` is `SecretStr | None`:
`smtp_password`, `ses_access_key_id`, `ses_secret_access_key`,
`twilio_auth_token`, `google_client_secret`. The Twilio Account SID
(`AC…`) is typed as plain `str`, which means it will appear unmasked in
`repr()` output, Pydantic model dumps, and exception tracebacks. One-line fix:
change to `SecretStr | None` and update the access site to `.get_secret_value()`.

### I-10 (carry-forward) — `GoogleProvider._client_secret` held as plain `str` in memory

**File:** `src/regstack/oauth/providers/google.py:88`

`GoogleProvider.__init__` receives `client_secret: str` and stores it as
`self._client_secret: str`. Once extracted from `SecretStr` at construction
in `app.py`, there is no in-memory masking. A crash dump or `__repr__` of the
provider object would include the secret in plain text. Adding a `__repr__`
override returning `GoogleProvider(client_id=..., client_secret=***)` provides
defense-in-depth against accidental log exposure.

### I-11 (carry-forward) — Lockout is per-email only; no per-IP protection

**File:** `src/regstack/auth/lockout.py`

`lockout.record_failure(email, ip=None)` — the `ip` argument is always `None`
in the login router. An attacker with a list of target email addresses can
distribute guesses across accounts, advancing each account's lockout counter
slowly while making effectively unlimited aggregate attempts. Recommendation:
document this gap in `docs/security.md` and add a configuration guidance note
in `docs/embedding.md` recommending proxy-layer IP rate limiting as a
complement to the per-account lockout.

---

## 🟢 CLEAN (Passed review)

### §1 — Dependency CVE scan

CVE search conducted across all 22 tracked packages for advisories published
2026-07-21 to 2026-07-22. **No new CVEs discovered in this window.**

Advisory search also confirmed that all previously known CVEs are fully
covered by the current `pyproject.toml` floors — no floor bumps required:

| CVE | Package | Fixed in | regstack floor | Locked version | Status |
|---|---|---|---|---|---|
| CVE-2026-48710 | starlette | 1.0.1 | >=1.3.1 | 1.3.1 | Clean |
| CVE-2026-54283 | starlette | 1.3.1 | >=1.3.1 | 1.3.1 | Clean |
| CVE-2026-54282 | starlette | 1.3.0 | >=1.3.1 | 1.3.1 | Clean |
| CVE-2026-48818 | starlette | 1.1.0 | >=1.3.1 | 1.3.1 | Clean |
| CVE-2026-58203 | pydantic-settings | 2.14.2 | >=2.14.2 | 2.14.2 | Clean |
| CVE-2026-32597 | pyjwt | 2.12.0 | >=2.13.0 | 2.13.0 | Clean |
| CVE-2026-48526 | pyjwt | 2.13.0 | >=2.13.0 | 2.13.0 | Clean |
| CVE-2026-40347 | python-multipart | 0.0.26 | >=0.0.32 | 0.0.32 | Clean |
| CVE-2026-42561 | python-multipart | 0.0.27 | >=0.0.32 | 0.0.32 | Clean |
| CVE-2026-53533–40 | python-multipart | 0.0.32 | >=0.0.32 | 0.0.32 | Clean |
| CVE-2026-34073 | cryptography | 46.0.6 | >=48.0.1 | 49.0.0 | Clean |
| CVE-2026-39892 | cryptography | 46.0.7 | >=48.0.1 | 49.0.0 | Clean |
| CVE-2026-45447 | cryptography | 48.0.1 | >=48.0.1 | 49.0.0 | Clean |
| CVE-2026-53533 | aiosmtplib | 5.1.1 | >=5.1.1 | 5.1.2 | Clean |

Note: CVE-2026-4984 (Twilio webhook signature bypass) affects a third-party
Botpress integration, not the `twilio` PyPI package itself — not applicable.

Full locked-version table (uv.lock unchanged from 2026-07-21):

| Package | Locked | Floor | Status |
|---|---|---|---|
| fastapi | 0.136.1 | >=0.120.0 | Clean |
| starlette | 1.3.1 | >=1.3.1 | Clean |
| pydantic | 2.x | >=2.6 | Clean |
| pydantic-settings | 2.14.2 | >=2.14.2 | Clean |
| pyjwt | 2.13.0 | >=2.13.0 | Clean |
| jinja2 | 3.1.6 | >=3.1.6 | Clean |
| python-multipart | 0.0.32 | >=0.0.32 | Clean |
| sqlalchemy | 2.0.49 | >=2.0 | Clean |
| alembic | 1.18.4 | >=1.13 | Clean |
| aiosqlite | 0.22.1 | >=0.20 | Clean |
| asyncpg | 0.31.0 | >=0.29 | Clean |
| pymongo | 4.17.0 | >=4.9 | Clean |
| pwdlib[argon2] | 0.3.0 | >=0.2.1 | Clean |
| cryptography | 49.0.0 | >=48.0.1 | Clean |
| aiosmtplib | 5.1.2 | >=5.1.1 | Clean |
| httpx | 0.28.1 | — | Clean |
| dnspython | 2.8.0 | >=2.6 | Clean |
| aioboto3 | 15.5.0 | >=12.3 | Clean |
| twilio | 9.10.5 | >=9.0 | Clean |
| click | — | >=8.1 | Clean |
| slowapi | — | >=0.1.9 | Clean |
| pywebview | — | >=5.0 | Clean |

Optional-extra hygiene: `oauth` adds only `pyjwt[crypto]` + `cryptography`.
`postgres` adds only `asyncpg`. `ses` and `sns` add only `aioboto3`. `twilio`
adds only `twilio`. No undeclared transitive dependencies. No yanked packages.

### §2 — Hardcoded secrets

No hardcoded secrets, credentials, private keys, or JWT bearer tokens found in
`src/`, `tests/`, `examples/`, or `docs/`. No `eyJ`-prefixed JWT strings in
any source files. `.gitignore` covers `.env`, `.env.*`, `regstack.secrets.env`,
`*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.jks`, `*.crt`, `htmlcov/`, `.coverage`.
`git log --diff-filter=A` on `*.env`, `*.key`, `*.pem`, `*secret*`,
`*credential*` returned only the historical `feat: explicit SES credential
fields (#69)` commit — no credential material. `git ls-files` found no
sensitive files except the expected `src/regstack/config/secrets.py` (key
derivation logic, not secret values).

### §3 — Authentication & Authorization

- All authenticated endpoints inject `current_user()` or `current_admin()`.
- All mutating endpoints require authentication.
- Login lockout fires before user lookup and password verification — no
  timing oracle. Verified in `src/regstack/routers/login.py:73`.
- `record_failure()` wired on all four failure paths (user-not-found,
  wrong-password, inactive, unverified).
- Password reset calls `update_password` + `lockout.clear` + JTI blacklist
  (`src/regstack/routers/password.py:114–117`).
- Password change bumps `tokens_invalidated_after` via `update_password`.
- Email-change confirm bumps `tokens_invalidated_after` via `users.update_email`.
  JTI replay gap tracked in I-5.
- Admin `PATCH /admin/users/{id}` with `is_active=false` calls
  `set_tokens_invalidated_after` (`src/regstack/routers/admin.py:111`).
- Account deletion cascades `oauth_identities` before deleting the user
  (`src/regstack/routers/account.py:229–231`). Admin deletion follows the
  same pattern (`src/regstack/routers/admin.py:134`).
- OAuth-only users (`hashed_password=None`) return the same generic 401 as a
  wrong-password attempt — no account enumeration via login
  (`src/regstack/routers/login.py:99–103`).
- Per-purpose JWT key derivation confirmed: `derive_secret(master, purpose)`
  in `src/regstack/config/secrets.py`.
- Constant-time password verification via `pwdlib` (Argon2id).

### §4 — Storage-layer injection safety

All SQL queries use SQLAlchemy Core with bound parameters. The two `text()`
usages are a health-check literal (`text("SELECT 1")` in `backend.py:75`)
and a static migration assertion in `0002_oauth.py:93` — neither accepts user
input. No f-string or `%`-interpolation into raw SQL anywhere. All MongoDB
queries use structured dict filter syntax. `ObjectId.is_valid()` guards all
user-ID-to-ObjectId conversions in `src/regstack/backends/mongo/repositories/
user_repo.py:54`. No `$where`, `$expr`, `$function`, or `$accumulator`
operators. `UtcDateTime` TypeDecorator in `src/regstack/backends/sql/types.py`
re-attaches UTC tzinfo on read. All SQL repos implement `purge_expired()`. No
`$in` arrays accept unbounded user input. No regex patterns built from user
input in any Mongo repository.

### §5 — Input validation & injection

`EmailStr` applied on all email-accepting models. `is_valid_e164()` called
before storing phone numbers (`src/regstack/routers/phone.py:61`). Jinja2
autoescape enabled in all template environments. Zero `| safe` filters in
`src/regstack/ui/templates/`. Zero `{% autoescape false %}` blocks. Email
templates have one `style=` attribute in `verification.html` — this is an
inline style in an HTML email body (not an SSR page), acceptable for email
clients that strip `<style>` blocks; it does not affect the CSP of the SSR
web pages. Field length bounds on all user-facing string fields. Open-redirect
protection in `_validate_redirect()` rejects backslash, protocol-relative,
and off-origin URLs with 400 (`src/regstack/routers/oauth.py:515–552`). CLI
wizard does not exec user input as shell commands.

### §6 — Rate limiting & DoS prevention

Per-route IP rate limiting is opt-in via `slowapi` config keys. Login lockout
fires on every failed attempt before password verification. MFA code brute-force
bounded by `max_attempts` with attempt tracking and row deletion on success.
OAuth state rows have a 5-minute TTL (`state_ttl_seconds=300`) with a Mongo
TTL index and SQL-side `purge_expired()`. Admin `/users` listing capped at
`limit: int = Query(ge=1, le=200)`. No file-upload endpoints exist. Per-IP
gap documented in I-11.

### §7 — Session & token security

- `iat` emitted as a float (`self.iat.timestamp()` at `jwt.py:69`) —
  microsecond-precision bulk-revoke comparison correct.
- pyjwt `exp`/`iat` checks disabled (`"verify_exp": False, "verify_iat":
  False`); all expiry validation uses the injected `Clock`.
- Algorithm restricted to `Literal["HS256", "HS384", "HS512"]` — `none`
  and asymmetric algorithms excluded by design.
- Required claims enforced: `["sub", "exp", "iat", "jti", "purpose"]`.
- `_authenticate` checks: creds present → decode → `is_revoked(jti)` →
  `get_by_id(sub)` is active → `is_payload_bulk_revoked(payload, cutoff)`.
- Token decode errors return a static `"Invalid or expired token."` — no
  pyjwt detail forwarded to callers.
- No token values in any log statement. OAuth exception logging at
  `routers/oauth.py:315,318,321` logs only exception messages (error
  codes / provider descriptions), not raw token strings.
- `jti` present in every token (`secrets.token_urlsafe(16)` at encode time).

### §8 — CSP and the SSR layer

Zero `style=` attributes, zero `<style>` blocks, zero inline `<script>` tags
in `src/regstack/ui/templates/`. All JS loaded via `<script src defer>`.
`oauth_complete.html` follows the same discipline. No user-supplied data
rendered server-side into HTML — `regstack.js` fills fields via API calls.
SSR templates are stateless (no `{{ user.email }}` or similar rendered
server-side into the HTML).

### §9 — Data exposure

`UserPublic.from_user()` never includes `hashed_password`. No router returns a
raw `BaseUser`. No log or error-response path echoes `jwt_secret`,
`client_secret`, or `smtp_password`. Exception handling in all routers returns
generic messages. pyjwt exception detail never forwarded to clients. OAuth
callback failures redirect to `/login?error=<short-code>` only; the provider
`error` query-string parameter is sanitised via `_sanitize_for_log()` before
logging. `tokens_invalidated_after` exposure in `UserPublic` tracked in I-1.

### §10 — Git history & recent changes

`git log --oneline -30` reviewed. One commit since 2026-07-21: the prior daily
security report (docs-only, `b35b2a3`). No commits touch `auth/`,
`config/secrets.py`, routers, or the JWT codec. `git log --diff-filter=A` on
`*.env`, `*.key`, `*.pem`, `*secret*`, `*credential*` returned only the
historical `feat: explicit SES credential fields (#69)` commit — no credential
material.

### §11 — Third-party integration security

- **SMTP:** `smtp_password` stored as `SecretStr`; extracted via
  `.get_secret_value()` immediately before send. Not logged.
- **SES:** `ses_access_key_id` and `ses_secret_access_key` are `SecretStr`.
  Credentials not logged.
- **SNS/Twilio:** `twilio_auth_token` is `SecretStr`. `twilio_account_sid` is
  plain `str` — tracked in I-9. Lazy imports confirmed.
- **Google OAuth:** `google_client_secret` is `SecretStr` in `OAuthConfig`.
  Extracted to plain `str` at `GoogleProvider` construction — tracked in I-10.
  JWKS timeout set to 5 seconds (`JWKS_FETCH_TIMEOUT_SECONDS = 5`). Fetch
  offloaded to `asyncio.to_thread`. Not logged.

### §12 — GitHub Actions workflow security

- **Workflows present:** `publish.yml`, `test.yml` only. No new workflows.
- **SHA pinning:** all `uses:` directives reference commit SHAs, not tags.
  `actions/checkout@de0fac2e4500...`, `astral-sh/setup-uv@08807647e706...`,
  `actions/upload-artifact@043fb46d1a93...`,
  `actions/download-artifact@3e5f45b2cfb9...`,
  `pypa/gh-action-pypi-publish@cef221092ed1...` — all pinned.
- **No `pull_request_target` trigger.** No self-hosted runners.
- **Permissions:** workflow default `contents: read`. `publish` job adds
  `id-token: write` only. No `write-all`.
- **No untrusted input** interpolated into `run:` blocks.
- **No `actions/cache`** — no cache-poisoning surface.
- **`workflow_dispatch`** absent from `publish.yml`. `test.yml` retains it
  (safe — no publish-relevant secrets accessible).
- **No `PYPI_API_TOKEN`** in any workflow. OIDC trusted publishing only.
- **Sigstore attestations:** `attestations: true` in `pypa/gh-action-pypi-publish`.

### §13 — PyPI package integrity

- **Wheel contents:** `[tool.hatch.build.targets.wheel]` includes only
  `src/regstack`. Tests, docs, examples, scripts, tasks excluded.
- **Sdist excludes:** `.git`, `.github/`, `CLAUDE.md`, `docs/`, `examples/`,
  `scripts/`, `tasks.py`, `tasks/`, `tests/`, `uv.lock` — all present in
  `[tool.hatch.build.targets.sdist] exclude`.
- **Tag-version verification:** reads from `pyproject.toml` via `tomllib`
  (not by importing the package).
- **Publication path:** OIDC trusted publishing, `id-token: write` only,
  trigger is tag push (`v*`), not branch push.
- **No localhost / test fixtures** in wheel contents.
- **No developer paths** (`/Users/`, `/home/`) in wheel contents.
- **Optional / dev-only deps** not in base `dependencies` list.
  `pymongo`, `asyncpg`, `aioboto3`, `twilio`, `pyjwt[crypto]` all in
  optional-dependencies only.
- **PyPI version:** 0.8.6 matches `pyproject.toml` and `version.py`.
  No out-of-band publishes detected.

### §14 — OAuth subsystem

- **PKCE:** `code_verifier` generated server-side (`secrets.token_urlsafe(64)`)
  and stored on the `oauth_states` row. Only `code_challenge` (SHA-256 of
  verifier) goes to the provider. Verified in `_begin_flow()`
  (`src/regstack/routers/oauth.py:598–624`).
- **State lifecycle:** callback checks missing/expired row; uses `rs.clock.now()`
  (not `datetime.now(UTC)`). `consume()` is atomic (read + delete) — a second
  exchange with the same id returns 404. State expiry check at
  `routers/oauth.py:303`.
- **ID-token verification:** validates `sub`, `iss`, `aud`, `exp`, `iat`,
  `nonce` via pyjwt. Algorithm restricted to `["RS256"]`. Nonce checked
  explicitly after decode (`google.py:209`).
- **Linking policy:** `email_in_use`, `identity_in_use`, `already_linked` all
  return distinct short error codes via redirect. `auto_link_verified_emails`
  defaults to `False`. Re-linking the same identity raises `already_linked`.
- **OAuth-only user side effects:** login, change-password, change-email, and
  delete-account all correctly handle `hashed_password=None`.
  `DELETE /oauth/{provider}/link` refuses if it's the last sign-in method and
  user has no password.
- **Identity uniqueness:** `(provider, subject_id)` and `(user_id, provider)`
  both have DB-level unique indexes on Mongo (`provider_subject_unique`,
  `user_provider_unique`) and SQL (`UniqueConstraint`).
- **Hooks:** `oauth_signin_started`, `oauth_signin_completed`,
  `oauth_account_linked`, `oauth_account_unlinked` confirmed in
  `hooks/events.py` and fired at the correct router points.

### §15 — Multi-backend parity & schema migration safety

- **CI matrix:** `test.yml` runs the full parametrized test suite against
  SQLite + Postgres + MongoDB on both Python 3.11 and 3.12. Three-backend
  coverage confirmed.
- **Migrations:** `0001_initial_schema.py`, `0002_oauth.py`,
  `0003_oauth_state_result_was_new.py` all have `upgrade()` and `downgrade()`.
  `0002_oauth.py` and `0003` use `batch_alter_table` for SQLite column-alter
  compatibility.
- **Autogen-drift test:** `tests/integration/test_sql_migrations.py` present
  and verified.
- **Mongo index parity:** `users.email` UNIQUE (`email_unique`),
  `oauth_identities (provider, subject_id)` UNIQUE (`provider_subject_unique`),
  `oauth_identities (user_id, provider)` UNIQUE (`user_provider_unique`) — all
  declared in `src/regstack/backends/mongo/indexes.py`. SQL-side
  `UniqueConstraint` entries match. TTL indexes on `expires_at` for all
  time-limited collections.
- **Datetime tz-awareness:** `make_client(tz_aware=True)` for Mongo,
  `UtcDateTime` TypeDecorator for SQL. Both confirmed present and correct.

---

## 📊 Summary

| | |
|---|---|
| **Date** | 2026-07-22 |
| **Version** | 0.8.6 |
| **🔴 Critical** | 0 |
| **🟠 Warning** | 0 |
| **🟡 Info** | 9 (all carry-forward; none new) |
| **🟢 Clean sections** | 15 of 15 structurally sound |

**vs. previous review (2026-07-21):** No new CVEs for any tracked package in
the July 21–22 window. Advisory sweep also confirmed all known CVEs (starlette
four-CVE cluster, pydantic-settings CVE-2026-58203, pyjwt cluster, python-
multipart six-CVE cluster, cryptography cluster, aiosmtplib CVE-2026-53533)
are fully addressed by the current `pyproject.toml` floors and locked versions.
One commit landed since the last review (docs-only security report). No findings
opened or closed. This is the third consecutive `[security-clean]` review.

**Top 3 priorities for the development team:**

1. **I-5 — Email-change token JTI replay** — one-liner fix matching the
   existing password-reset pattern; adds `await rs.blacklist.revoke(jti, exp)`
   in `confirm_email_change` (`src/regstack/routers/account.py:202`). Low
   impact but simple to close.

2. **I-6 — Attempts-remaining count in 401 body** — remove the attempt count
   from `_build_invalid()` (`src/regstack/routers/login.py:42`); return a
   static `"Invalid email or password."` to remove the pacing oracle.

3. **I-9 / I-10 — Credential masking** — change `twilio_account_sid` from
   `str | None` to `SecretStr | None` in `config/schema.py:98`, and add a
   `__repr__` override on `GoogleProvider` (`oauth/providers/google.py:88`)
   to prevent accidental exposure in tracebacks and log output.
