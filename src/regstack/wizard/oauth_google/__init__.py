"""Google OAuth setup wizard (``regstack oauth setup``).

Walks a host operator through registering a Google OAuth 2.0 client
and merging the resulting credentials into ``regstack.toml`` +
``regstack.secrets.env``. Runs in a native webview window.
"""
