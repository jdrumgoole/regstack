# Security Review — 2026-07-23

**Reviewer:** Automated security agent (scheduled daily review)
**Version reviewed:** 0.8.6 (`src/regstack/version.py`)
**Scope:** Full 15-section review per `scripts/security-review-prompt.md`
**Previous review:** 2026-07-22 (0 critical, 0 warning, 9 INFO — `[security-clean]`)

> **Since the last review (2026-07-22):** One commit landed on `main`:
>
> - `d626053 [security-clean] Security Review — 2026-07-22` — docs-only, the
>   previous day's report. No product code changes.
>
> **Net change in findings:** CVE sweep for July 22–23 found no new advisories
> for any tracked package. No code changes; no findings opened or closed.
> This is the fourth consecutive `[security-clean]` review.

---

## 🔴 CRITICAL (Immediate action required)

None.

---

## 🟠 WARNING (Address within 1 week)

None.

---

## 🟡 INFO (Best practice recommendations)

All nine carry-forward items from 2026-07-22 remain open, unchanged. No new INFO items.

### I-1 (carry-forward) — `UserPublic` exposes `tokens_invalidated_after`

`UserPublic` includes `tokens_invalidated_after: datetime | None`
(`src/regstack/models/user.py:105`). This is an intentional design choice
(SPAs compare it to the session JWT `iat` to detect bulk-revocation without
an extra round-trip). The exposure leaks the exact timestamp of the most
recent security event (password change, email change, admin-disable) to any
authenticated client. Acceptable for regstack defaults; hosts with stricter
data-minimisation requirements should subclass `UserPublic` or project out
the field. Recommend documenting the trade-off explicitly in `docs/security.md`.

### I-2 (carry-forward) — `admin_promote_pending` path parameter not validated as `EmailStr`

`POST /admin/pending/{email}/promote` (`src/regstack/routers/admin.py:127`)
accepts `email: str = Path(...)`. A syntactically invalid email address passes
parameter binding and returns 404. The endpoint is admin-gated so risk is low.
One-line fix: `email: Annotated[EmailStr, Path(...)]`.

### I-5 (carry-forward) — Email-change token JTI not blacklisted on first use

**File:** `src/regstack/routers/account.py:175` (`confirm_email_change`)

`POST /confirm-email-change` does not blacklist the token's `jti` after
successfully completing the email swap. The password-reset handler does
blacklist its token's `jti` at `src/regstack/routers/password.py:71`. The
email-change token has a 1-hour TTL (default). An attacker who captures the
URL from browser history, proxy logs, or email forwarding can replay it within
that window; `update_email()` accepts the second call silently (same user,
same email — no constraint violation) and bumps `tokens_invalidated_after`,
forcing the user to re-authenticate.

The practical impact is low: no email hijacking is possible (the target address
is already set), but the attacker can cause repeated forced re-logins until
token expiry. The fix is a one-liner matching the reset-password pattern:
call `await rs.blacklist.revoke(jti, exp)` after `update_email()` succeeds.

### I-6 (carry-forward) — "X attempt(s) remaining" count in 401 response body

**File:** `src/regstack/routers/login.py:42`

`_build_invalid()` includes the remaining failure count in the 401 detail
message: `f"Invalid email or password. {remaining} attempt(s) remaining."`.
This tells a credential-stuffing attacker exactly when to pause to avoid
lockout, enabling systematic pacing of brute-force attempts to stay under
the threshold indefinitely. Recommendation: return a static
`"Invalid email or password."` message regardless of remaining attempts.

### I-7 (carry-forward) — `jwt_audience` defaults to `None`; no multi-instance deployment guidance

**File:** `src/regstack/config/schema.py:189`

With `jwt_audience = None`, pyjwt skips audience validation on decode. If two
regstack instances share the same `jwt_secret` (a microservice topology where
one JWT secret is shared across services), a session token issued by instance A
would be accepted by instance B. No audience guidance appears in
`docs/configuration.md` or the examples. Recommendation: add a configuration
note advising hosts to set `jwt_audience` to a service-unique string in any
deployment where multiple regstack instances share a `jwt_secret`.

### I-8 (carry-forward) — No HTTP security headers set or demonstrated

**Scope:** SSR pages served by `ui_router`

regstack does not set `Content-Security-Policy`, `X-Frame-Options`,
`X-Content-Type-Options`, or `Referrer-Policy` headers. This is expected for
an embedded library — host middleware owns the response headers. However,
neither the `examples/` demos nor `docs/embedding.md` show a recommended
middleware stack for hosts that enable `ui_router`. Recommendation: add a
"Security hardening" section to the embedding guide showing a simple
security-headers middleware configured for the regstack SSR prefix.

### I-9 (carry-forward) — `twilio_account_sid` typed as `str | None` rather than `SecretStr`

**File:** `src/regstack/config/schema.py:98`

Every other credential field in `RegStackConfig` is `SecretStr | None`:
`smtp_password`, `ses_access_key_id`, `ses_secret_access_key`,
`twilio_auth_token`, `google_client_secret`. The Twilio Account SID
(`AC…`) is typed as plain `str`, which means it will appear unmasked in
`repr()` output, Pydantic model dumps, and exception tracebacks. One-line fix:
change to `SecretStr | None` and update the access site to `.get_secret_value()`.

### I-10 (carry-forward) — `GoogleProvider._client_secret` held as plain `str` in memory

**File:** `src/regstack/oauth/providers/google.py:88`

`GoogleProvider.__init__` receives `client_secret: str` and stores it as
`self._client_secret: str`. Once extracted from `SecretStr` at construction
in `app.py`, there is no in-memory masking. A crash dump or `__repr__` of the
provider object would include the secret in plain text. Adding a `__repr__`
override returning `GoogleProvider(client_id=..., client_secret=***)` provides
defense-in-depth against accidental log exposure.

### I-11 (carry-forward) — Lockout is per-email only; no per-IP protection

**File:** `src/regstack/auth/lockout.py`

`lockout.record_failure(email, ip=None)` — the `ip` argument is always `None`
in the login router. An attacker with a list of target email addresses can
distribute guesses across accounts, advancing each account's lockout counter
slowly while making effectively unlimited aggregate attempts. Recommendation:
document this gap in `docs/security.md` and add a configuration guidance note
in `docs/embedding.md` recommending proxy-layer IP rate limiting as a
complement to the per-account lockout.

---

## 🟢 CLEAN (Passed review)

### §1 — Dependency CVE scan

CVE sweep conducted across all 22 tracked packages for advisories published
2026-07-22 to 2026-07-23. **No new CVEs discovered in this window.**

Known CVEs previously addressed by `pyproject.toml` floors remain covered;
locked versions in `uv.lock` are unchanged from 2026-07-22:

| Package | Locked | Floor | Status |
|---|---|---|---|
| fastapi | 0.136.1 | >=0.120.0 | Clean |
| starlette | 1.3.1 | >=1.3.1 | Clean |
| pydantic | 2.13.3 | >=2.6 | Clean |
| pydantic-settings | 2.14.2 | >=2.14.2 | Clean |
| pyjwt | 2.13.0 | >=2.13.0 | Clean |
| jinja2 | 3.1.6 | >=3.1.6 | Clean |
| python-multipart | 0.0.32 | >=0.0.32 | Clean |
| sqlalchemy | 2.0.49 | >=2.0 | Clean |
| alembic | 1.18.4 | >=1.13 | Clean |
| aiosqlite | 0.22.1 | >=0.20 | Clean |
| asyncpg | 0.31.0 | >=0.29 | Clean |
| pymongo | 4.17.0 | >=4.9 | Clean |
| pwdlib[argon2] | 0.3.0 | >=0.2.1 | Clean |
| cryptography | 49.0.0 | >=48.0.1 | Clean |
| aiosmtplib | 5.1.2 | >=5.1.1 | Clean |
| httpx | 0.28.1 | — | Clean |
| dnspython | 2.8.0 | >=2.6 | Clean |
| aioboto3 | 15.5.0 | >=12.3 | Clean |
| twilio | 9.10.5 | >=9.0 | Clean |
| click | — | >=8.1 | Clean |
| slowapi | — | >=0.1.9 | Clean |
| pywebview | — | >=5.0 | Clean |

Notes on specific advisories confirmed again this cycle:
- **CVE-2026-2978 / CVE-2026-2979** (mentioned in search results): affect the
  third-party "FastApiAdmin" project (separate PyPI package), not the `fastapi`
  package itself. `pyproject.toml` comment at line 6 documents this. No floor
  bump needed. Carry-forward confirmed.
- **CVE-2026-48710 (BadHost — starlette)**: fixed in 1.0.1; regstack floor is
  >=1.3.1, locked at 1.3.1. Clean.
- **CVE-2026-48526 (pyjwt algorithm confusion)**: fixed in 2.13.0; regstack
  floor is >=2.13.0, locked at 2.13.0. Clean.
- No new SQLAlchemy, Alembic, PyMongo, AsyncPG, Twilio, dnspython, or pwdlib
  advisories surfaced for July 2026.

Optional-extra hygiene confirmed: `oauth` adds only `pyjwt[crypto]` +
`cryptography`; `postgres` only `asyncpg`; `ses`/`sns` only `aioboto3`;
`twilio` only `twilio`. No undeclared transitive dependencies. No yanked packages.

### §2 — Hardcoded secrets

No hardcoded secrets, credentials, private keys, or JWT bearer tokens found in
`src/`, `tests/`, `examples/`, or `docs/`. One `eyJ`-prefixed JWT string in
`tests/integration/test_happy_path.py:103` is a clearly-invalid test fixture
(`"invalid-signature"` suffix). `.gitignore` covers `.env`, `.env.*`,
`regstack.secrets.env`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.jks`, `*.crt`,
`htmlcov/`, `.coverage`. `git log --diff-filter=A` on credential-pattern
filenames returned only the historical config refactor commit — no secrets
material. Example `regstack.toml` files in `examples/` use only placeholder
values (`noreply@example.com`, `console` backend, no real credentials).

### §3 — Authentication & Authorization

- All authenticated endpoints inject `current_user()` or `current_admin()`.
- All mutating endpoints require authentication.
- Login lockout fires before user lookup and password verification — no
  timing oracle. Verified in `src/regstack/routers/login.py:73`.
- `AuthDependencies._authenticate` enforces the full revocation chain:
  decode → `blacklist.is_revoked(jti)` → `users.get_by_id` →
  `is_active` check → `is_payload_bulk_revoked` check.
- Password reset calls `update_password` (bumps `tokens_invalidated_after`)
  AND `lockout.clear` AND blacklists the reset token's jti. Correct.
- Password change calls `update_password` (bumps `tokens_invalidated_after`)
  AND `lockout.clear`. Correct.
- Email-change confirm calls `update_email` (bumps `tokens_invalidated_after`).
  Does NOT blacklist the email-change jti — carry-forward I-5.
- Admin `PATCH /admin/users/{id}` with `is_active=False` calls
  `set_tokens_invalidated_after`. Correct.
- Account deletion cascades `oauth_identities.delete_by_user_id()` before
  `users.delete()`. Same cascade applied in `DELETE /admin/users/{id}`. Correct.
- OAuth-only users (`hashed_password=None`): login returns generic 401,
  `change-password`/`change-email`/`delete-account` correctly invoke
  `require_password_set()` which returns 400 pointing at password-reset.
- Password comparison via `pwdlib`'s Argon2 verify — not `==`. Correct.
- JWT keys derived per-purpose via `derive_secret(jwt_secret, purpose)` in
  `src/regstack/config/secrets.py`. HMAC-SHA256. Compromise of one derived
  key does not expose others or the master. Correct.

### §4 — Storage-layer injection

**MongoDB:** All query filters use literal dict keys with bound values (no
f-string or `%` interpolation). `ObjectId.is_valid()` guards every ID lookup
before `ObjectId(user_id)` construction — verified in `user_repo.py`,
`oauth_identity_repo.py`. No `$where`, `$expr`, `$function`, or
`$accumulator` operators in any repository. No user-input regex construction.
No unbounded `$in` arrays from user input.

**SQL:** All queries use SQLAlchemy Core parameterised binds (`select`,
`update`, `delete`, `insert`). Two `text()` usages: `text("SELECT 1")` in
`sql/backend.py` (health check, no user input) and
`text("SELECT COUNT(*) FROM users WHERE hashed_password IS NULL")` in
migration `0002_oauth.py` (hardcoded SQL, no user input). Both safe.

`UtcDateTime` TypeDecorator in `src/regstack/backends/sql/types.py` correctly
re-attaches UTC tzinfo on read via `process_result_value`. Both
`process_bind_param` and `process_result_value` are implemented. `cache_ok =
True` set correctly.

### §5 — Input validation & injection

- All API endpoints accept typed Pydantic models; no raw `dict` or untyped
  JSON body inputs found.
- `EmailStr` on all email-bearing fields (`UserCreate`, `ForgotPasswordRequest`,
  `ChangeEmailRequest`, `LoginRequest`, `RegisterRequest`). Confirmed.
- E.164 validation via `is_valid_e164()` in `src/regstack/sms/base.py` called
  on every phone input before storage (`phone.py:52`). Confirmed.
- Email composer (`MailComposer._build_env`) uses `autoescape=select_autoescape(["html"])`.
  No `{% autoescape false %}` in any template. No `| safe` filter in any UI
  or email template.
- UI templates contain no `{{ user.email }}` or similar auth-state rendering —
  pages are stateless as documented. Confirmed.
- OAuth `redirect_to` validation (`_validate_redirect`) rejects backslashes,
  protocol-relative URLs (`//host`), and off-origin full URLs. Confirmed.
- CLI wizard inputs do not feed into shell commands — no `subprocess` / `os.system`
  calls in `src/regstack/wizard/`. Confirmed.

### §6 — Rate limiting & DoS prevention

- Login lockout wired correctly: `lockout.check()` fires before user lookup;
  `record_failure` called on every rejection path including is_active and
  is_verified branches.
- `login_attempts` MongoDB TTL index sized to `login_lockout_window_seconds`
  (`src/regstack/backends/mongo/indexes.py`). SQL read-side filtering via
  `count_recent()`. Confirmed.
- Admin `GET /admin/users` caps `limit` at 200 via `Query(ge=1, le=200)`.
  No unbounded pagination endpoint found.
- OAuth state rows have TTL index on `expires_at` on Mongo (`oauth_state_ttl`
  in `indexes.py`); SQL backend uses `expires_at` index for read-side
  expiry filtering.
- No user-facing file upload endpoints added.

### §7 — Session & token security

- HS256 by default, secret via `config.jwt_secret` (`SecretStr`), ≥32 chars
  enforced at construction in `JwtCodec.__init__` (raises `ValueError` if empty).
- Every JWT includes `jti` (`secrets.token_urlsafe(16)`). Per-token revocation
  confirmed to work for logout and password reset.
- `iat` emitted as `float` (`payload.iat.timestamp()`) — RFC 7519 NumericDate
  float. `is_payload_bulk_revoked` uses `<=` comparison. `exp` decoded as `int`.
  Bulk-revoke precision maintained. Confirmed in `src/regstack/auth/jwt.py:60`.
- `JwtCodec.decode` disables pyjwt `exp`/`iat`/`nbf` checks and re-validates
  both against the injected Clock. `FrozenClock` seam intact. Confirmed.
- `_authenticate` enforces the full chain: decode → blacklist → user active →
  bulk-revoke. Confirmed in `src/regstack/auth/dependencies.py`.
- No token material found in `log.info` / `log.error` / `log.exception` calls.
  OAuth error bodies logged at DEBUG (not WARNING) to avoid verbose provider
  response exposure.

### §8 — CSP and the SSR layer

- No `<style>` blocks or `style="..."` attributes in any file under
  `src/regstack/ui/templates/`. Inline email styles in
  `src/regstack/email/templates/verification.html` are in email templates
  (not the SSR layer) and are acceptable for email-client compatibility.
- Only one `<script>` tag found in any UI template:
  `<script src="{{ static_prefix }}/js/regstack.js" defer></script>` in
  `base.html` — external, deferred, no inline content. CSP-safe.
- `oauth_complete.html` (`src/regstack/ui/templates/auth/oauth_complete.html`)
  confirmed present and follows the same no-inline-script discipline.
- No template renders `{{ user.email }}` or any auth-state expression —
  all pages are stateless HTML.

### §9 — Data exposure

- All endpoints return `UserPublic.from_user(user)` — never raw `BaseUser`.
  `UserPublic` excludes `hashed_password`. `tokens_invalidated_after` is
  included (documented design choice — see I-1).
- JWT decode failures → generic `"Invalid or expired token."` 401; specific
  pyjwt exception text not echoed to the caller.
- OAuth callback failures → redirect to `/login?error=<short-code>` with
  a short error code (e.g., `bad_state`, `token_exchange_failed`). Underlying
  exceptions logged at WARNING/INFO, not echoed to redirect.
- Provider `error` callback query string sanitized via `_sanitize_for_log()`
  before logging — strips control characters, caps at 200 chars.
- No `f"... {exc}"` patterns in HTTP error response bodies found in any router.
- SMTP password via `config.smtp_password.get_secret_value()`. Google
  `client_secret` stored as `SecretStr` in config; extracted to plain `str`
  at `GoogleProvider` construction (see I-10). AWS keys via `SecretStr`.

### §10 — Git history & recent changes

`git log --oneline -30` reviewed. No commits to auth-sensitive files
(`auth/`, `config/secrets.py`, `routers/`, `oauth/`, `backends/`) since
the 0.8.6 release commit (`531a521`). No secrets material found via
`git log --all --diff-filter=A` on credential-pattern filenames. No recent
CVEs from §1 affect any dependency version touched by recent commits.

### §11 — Third-party integration security

- **SMTP:** `smtp_password` is `SecretStr | None`; `.get_secret_value()` used
  at send time. No echo in logs or errors. Confirmed in `email/smtp.py`.
- **SES:** AWS region and profile from config; `ses_access_key_id` and
  `ses_secret_access_key` are `SecretStr | None`. Lazy `aioboto3` import.
- **Twilio:** `twilio_auth_token` is `SecretStr`. `twilio_account_sid` is
  plain `str` — carry-forward I-9. Lazy import confirmed.
- **Google OAuth:** `google_client_secret` is `SecretStr` in config;
  extracted to `str` inside `GoogleProvider` — carry-forward I-10.
  `PyJWKClient` constructed with `timeout=5` (`JWKS_FETCH_TIMEOUT_SECONDS`).
  Fetch pushed to `asyncio.to_thread` so JWKS cache miss doesn't block the
  event loop. Confirmed in `oauth/providers/google.py`.

### §12 — GitHub Actions workflow security

Two workflows: `publish.yml` and `test.yml`. No new workflows detected.

**§12a — Third-party action pinning:** All `uses:` directives reference commit
SHAs, not mutable version tags:
- `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` (v6.0.2) ×3
- `astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b` (v8.1.0) ×3
- `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (v7.0.1) ×2
- `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1) ×1
- `pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b` (v1.14.0) ×1

All SHA-pinned. Clean.

**§12b — Untrusted input injection:** No `${{ github.event.* }}` interpolations
found in any `run:` block. No pull request title, body, head_ref, or comment
body expanded into shell. Clean.

**§12c — Permissions:** Workflow-level `permissions: contents: read` declared on
both workflows. `publish` job overrides to `id-token: write` + `contents: read`
for OIDC. `build` job retains `contents: read`. `test.yml` job retains
`contents: read`. No `write-all` or over-broad grants. Clean.

**§12d — `pull_request_target`:** Not present in any workflow. Clean.

**§12e — Self-hosted runners:** All jobs use `ubuntu-latest`. No
`runs-on: [self-hosted, ...]`. Clean.

**§12f — Secrets exposure:** No `secrets.*` references in either workflow.
No `PYPI_API_TOKEN`. OIDC trusted publishing only. No `echo "::add-mask::"`.
No `if: secrets.X != ''`. Clean.

**§12g — Cache poisoning:** No `actions/cache` usage in any workflow.
`astral-sh/setup-uv` with `enable-cache: true` manages a uv dependency cache
keyed on `uv.lock`, not user-controlled input. Clean.

**§12h — Deploy authorisation:** `publish.yml` trigger is `on: { push: { tags: ['v*'] } }`.
Previous `workflow_dispatch:` trigger removed (noted in the workflow comment).
Tag push is the only documented trigger. Clean.

### §13 — PyPI package integrity

- **Wheel contents:** `[tool.hatch.build.targets.wheel]` includes only
  `src/regstack`. Tests, docs, examples, scripts, tasks excluded.
- **Sdist excludes:** `.git`, `.github/`, `CLAUDE.md`, `docs/`, `examples/`,
  `scripts/`, `tasks.py`, `tasks/`, `tests/`, `uv.lock` — all listed in
  `[tool.hatch.build.targets.sdist] exclude`. `.git` explicitly excluded
  to avoid worktree `.git` file leaking developer home path.
- **Tag-version verification:** reads from `pyproject.toml` via `tomllib`,
  not by importing the package (avoids the 0.2.0 regression pattern).
- **Publication path:** OIDC trusted publishing, `id-token: write` only.
  `attestations: true` — PEP 740 sigstore attestations enabled.
  Trigger is tag push (`v*`), not branch push.
- **Version parity:** `pyproject.toml` version = 0.8.6, `version.py` = 0.8.6.
  No version mismatch. No out-of-band PyPI publishes detected.
- **Optional / dev-only deps:** `pymongo`, `asyncpg`, `aioboto3`, `twilio`,
  `pyjwt[crypto]` all in optional-dependencies only. Regression test at
  `tests/unit/test_base_install_imports.py` confirmed present.
- **Base-install smoketest job:** `base-install-smoketest` in `test.yml`
  builds the wheel, installs into a clean venv (no extras), and runs both
  an import check and a SQLite end-to-end smoketest. Confirmed present.
- No `localhost`, `127.0.0.1`, `alice@example.com`, or `/Users/`/`/home/`
  paths found in `src/regstack/` — nothing of the sort would appear in the
  built wheel.

### §14 — OAuth subsystem

- **PKCE:** `code_verifier` is `secrets.token_urlsafe(64)`, stored on the
  `oauth_states` row. Only `code_challenge` (SHA-256, URL-safe base64, no
  padding) sent to the provider. Verified in `_begin_flow()`
  (`src/regstack/routers/oauth.py:571`). Clean.
- **State lifecycle:** Callback checks missing/expired row; uses `rs.clock.now()`
  (not `datetime.now(UTC)`) for expiry. `oauth_states.consume()` is atomic
  (read + delete) — a second exchange returns 404. Confirmed.
- **ID-token verification (Google):** validates `sub`, `iss`, `aud`, `exp`,
  `iat`, `nonce` via `pyjwt.decode`. Algorithm restricted to `["RS256"]`.
  Nonce checked explicitly after decode (`google.py:202`). `PyJWKClient`
  timeout = 5 s. Any failure raises `OAuthIdTokenError`; inner detail logged,
  outer redirect uses short error code only.
- **Linking policy:** `email_in_use`, `identity_in_use`, `already_linked`,
  `user_inactive` all return short codes via redirect. `auto_link_verified_emails`
  defaults to `False`. Re-linking the same identity to the same user raises
  `already_linked`. Linking to a different user raises `identity_in_use`. Both
  protect against silent ownership transfer.
- **OAuth-only user side effects:** Login returns generic 401 for
  `hashed_password=None`. `change-password`, `change-email`, `delete-account`
  invoke `require_password_set()`. `DELETE /oauth/{provider}/link` refuses if
  it's the last sign-in method and user has no password. Confirmed.
- **Identity uniqueness:** `(provider, subject_id)` and `(user_id, provider)`
  both unique at DB level — Mongo indexes `provider_subject_unique` and
  `user_provider_unique` in `indexes.py`; SQL `UniqueConstraint` in
  `schema.py`. Clean.
- **Hooks:** `oauth_signin_started`, `oauth_signin_completed`,
  `oauth_account_linked`, `oauth_account_unlinked` all present in
  `hooks/events.KNOWN_EVENTS` and fired at the correct router points. Clean.
- **`allow_registration=False` respected:** `_resolve_user` raises
  `_LinkConflictError("registration_disabled")` before creating a new OAuth
  account when `config.allow_registration=False`. Clean.

### §15 — Multi-backend parity & schema migration safety

- **CI matrix:** `test.yml` runs the full parametrized suite against SQLite +
  PostgreSQL + MongoDB on both Python 3.11 and 3.12. Three-backend coverage
  confirmed. `inv test-all` is the release gate per CLAUDE.md.
- **Migrations:** `0001_initial_schema.py`, `0002_oauth.py`, and
  `0003_oauth_state_result_was_new.py` all have `upgrade()` and `downgrade()`.
  `0002` and `0003` use `batch_alter_table` for SQLite column-alter
  compatibility. `0002.downgrade()` includes the NULL-count guard to prevent
  silent data loss on SQLite.
- **Autogen-drift test:** `tests/integration/test_sql_migrations.py` confirmed
  present and exercises schema parity.
- **Mongo index parity:**
  - `users.email` UNIQUE → `email_unique` index in `indexes.py`. ✓
  - `oauth_identities (provider, subject_id)` UNIQUE → `provider_subject_unique`. ✓
  - `oauth_identities (user_id, provider)` UNIQUE → `user_provider_unique`. ✓
  - TTL indexes on `expires_at` for `pending_registrations`, `token_blacklist`,
    `login_attempts`, `mfa_codes`, `oauth_states`. All present. ✓
- **Datetime tz-awareness:** `make_client(tz_aware=True)` for Mongo
  (`backends/mongo/client.py`). `UtcDateTime` TypeDecorator with
  `process_result_value` re-attaches UTC for SQL (`backends/sql/types.py`).
  Both confirmed correct. Clean.

---

## 📊 Summary

| | |
|---|---|
| **Date** | 2026-07-23 |
| **Version** | 0.8.6 |
| **🔴 Critical** | 0 |
| **🟠 Warning** | 0 |
| **🟡 Info** | 9 (all carry-forward; none new) |
| **🟢 Clean sections** | 15 of 15 structurally sound |

**vs. previous review (2026-07-22):** No new CVEs for any tracked package in
the July 22–23 window. Confirmed CVE-2026-2978/2979 remain non-applicable
(FastApiAdmin third-party project, not fastapi). One commit landed since the
last review (docs-only security report). No findings opened or closed.
This is the fourth consecutive `[security-clean]` review.

**Top 3 priorities for the development team:**

1. **I-5 — Email-change token JTI replay** — one-liner fix matching the
   existing password-reset pattern; adds `await rs.blacklist.revoke(jti, exp)`
   after `update_email()` in `confirm_email_change`
   (`src/regstack/routers/account.py`). Low impact but trivial to close.

2. **I-6 — Attempts-remaining count in 401 body** — remove the attempt count
   from `_build_invalid()` (`src/regstack/routers/login.py:42`); return a
   static `"Invalid email or password."` to remove the pacing oracle.

3. **I-9 / I-10 — Credential masking** — change `twilio_account_sid` from
   `str | None` to `SecretStr | None` in `config/schema.py:98`, and add a
   `__repr__` override on `GoogleProvider` (`oauth/providers/google.py`)
   to prevent accidental secret exposure in tracebacks and log output.
