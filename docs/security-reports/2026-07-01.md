# Security Review — 2026-07-01

**Reviewer:** Automated security agent (scheduled daily review)
**Version reviewed:** 0.8.4 (`src/regstack/version.py`)
**Scope:** Full 15-section review per `scripts/security-review-prompt.md`
**Previous review:** 2026-06-29 (5 warnings, 0 critical)

> **Since the last review (2026-06-29):** One commit landed on `main` —
> `4ce11d9 docs(security-review): 2026-06-29 security review — 5 warnings, 0 critical`
> (the report itself). No code changes. All five warnings from 2026-06-29 remain
> open and unresolved. No new CVEs were identified in the 2026-06-30 to 2026-07-01
> window for the packages under review.

---

## 🔴 CRITICAL (Immediate action required)

None.

---

## 🟠 WARNING (Address within 1 week)

All five warnings carry forward from 2026-06-29 with no change in status.

### W-1 — Four CVEs in `python-multipart <0.0.32` (DoS + smuggling cluster) — carry-forward

**Package:** `python-multipart`
**Floor:** `>=0.0.27` · **Resolved (lock):** `0.0.28` · **Safe floor:** `>=0.0.32`
**Tracked:** GitHub issues #118 (open — duplicate) and #120 (open)

The current lock (`0.0.28`) lies in the vulnerable range for three of the four CVEs. Consumers
who run `pip install regstack` can resolve any version `>=0.0.27`, including all vulnerable ones.

| CVE | CVSS | Fixed in | Description |
|---|---|---|---|
| CVE-2026-53537 | 3.1 Low | 0.0.30 | RFC 2231/5987 Content-Disposition decoding mismatch enables header smuggling. |
| CVE-2026-53538 | 3.7 Low | 0.0.30 | `;` treated as field separator (WAF mismatch), enabling parameter smuggling. |
| CVE-2026-53539 | **7.5 High** | 0.0.30 | `QuerystringParser` O(B²) algorithm with `;` separators — unauthenticated CPU DoS. |
| CVE-2026-53540 | 3.7 Low | **0.0.31** | Negative `Content-Length` turns bounded read into read-until-EOF — memory DoS. |

**Action required:**
1. Bump `"python-multipart>=0.0.32"` in `pyproject.toml`. Update inline CVE comment to enumerate
   all four CVEs.
2. Run `uv lock`.
3. Close issue #118 as duplicate of #120; update #120 to enumerate all four CVEs.

---

### W-2 — Starlette floor gap covers four CVEs — carry-forward

**Package:** `starlette`
**Floor:** `>=1.0.1` · **Resolved (lock):** `1.3.1` · **Safe floor:** `>=1.3.1`
**Tracked:** GitHub issue #121

The lock already resolves `1.3.1` (fully patched), so the current development environment is
not at runtime risk. The danger is downstream: `pyproject.toml`'s `>=1.0.1` floor permits any
`pip install regstack` consumer to resolve starlette `1.0.1`–`1.3.0`, which contains three
unpatched CVEs (CVE-2026-48818, CVE-2026-54282, CVE-2026-54283).

| CVE | CVSS | Fixed in | Description |
|---|---|---|---|
| CVE-2026-48710 | 6.5–7.0 | 1.0.1 | "BadHost" — malformed `Host` header bypasses path-based auth. Documented in `pyproject.toml`. |
| CVE-2026-48818 | **7.5 High** | 1.1.0 | Windows: `StaticFiles` UNC path leaks NTLMv2 hash. |
| CVE-2026-54282 | 3.7 Low | 1.3.0 | `request.url.hostname` / `.netloc` attacker-controlled via non-`/` path. |
| CVE-2026-54283 | **High** | 1.3.1 | `x-www-form-urlencoded` ignores `max_fields`/`max_part_size` — DoS via 1M fields or 50 MB. |

**Action required:**
1. Bump `"starlette>=1.3.1"` in `pyproject.toml`. Update the inline comment to list all four CVEs.
2. Run `uv lock` (likely no-op since lock already resolves 1.3.1).
3. Update issue #121 to enumerate all four CVEs.

---

### W-3 — CVE-2026-45447: `cryptography` bundled OpenSSL heap UAF (CVSS 7.5 High) — carry-forward

**Package:** `cryptography` (in `oauth` optional extra: `cryptography>=46.0.7`)
**Resolved (lock):** `47.0.0` · **Safe version:** `>=48.0.1`
**Advisory:** GHSA-537c-gmf6-5ccf · Published: June 9, 2026
**Tracked:** GitHub issue #123

All PyPI `cryptography` wheels `>=0.5.0,<48.0.1` include a statically-linked OpenSSL version
with a heap use-after-free in `PKCS7_verify()` (empty `digestAlgorithms` SET in a PKCS#7/S/MIME
message). Regstack's `PyJWKClient` path uses RSA/EC key operations and does not call
`PKCS7_verify()` directly, but:
- The `oauth` extra floor `>=46.0.7` allows consumers to resolve `47.0.0` (vulnerable).
- Hosts that use `cryptography` for TLS cert handling or S/MIME alongside regstack are
  directly exposed.

**Action required:**
1. Bump `oauth` extra: `"cryptography>=48.0.1"` with updated CVE comment in `pyproject.toml`.
2. Bump `dev` extra: `"cryptography>=48.0.1"`.
3. Run `uv lock`.

---

### W-4 — CVE-2026-53533: `aiosmtplib 5.1.0` SMTP command injection (CVSS 6.5 Moderate) — carry-forward

**Package:** `aiosmtplib` (base dependency: `aiosmtplib>=3.0`)
**Resolved (lock):** `5.1.0` · **Safe version:** `>=5.1.1`
**Advisory:** GHSA-v3q9-hj7j-63hq · Published: June 15, 2026
**Tracked:** GitHub issue #124

The locked version (`5.1.0`) is the vulnerable version. `5.1.1` rejects C0 control characters
(CR, LF, NUL, etc.) in SMTP command arguments. Prior versions accept them, allowing SMTP injection
via a crafted email address such as `attacker@evil.com\r\nBcc: victim@example.com`, which can
silently add blind-copy recipients to verification, password-reset, or MFA emails.

This is directly on regstack's security-critical email delivery paths.

**Action required:**
1. Bump `"aiosmtplib>=5.1.1"` in `pyproject.toml` with updated CVE comment.
2. Run `uv lock`.

---

### W-5 — CVE-2026-32690: `pydantic-settings 2.14.0` symlink path traversal (CVSS 5.9 Medium) — carry-forward

**Package:** `pydantic-settings` (base dependency: `pydantic-settings>=2.2`)
**Resolved (lock):** `2.14.0` · **Safe version:** `>=2.14.2`
**Advisory:** DailyCVE DC-Jun2026-536 · Published: June 2026
**Tracked:** GitHub issue #125

The locked version (`2.14.0`) is in the vulnerable range (`2.12.0`–`2.14.1`). When a
`BaseSettings` subclass reads from a secrets directory, the size-check does not follow symlinks
but the file-loader does. An attacker with write access to the secrets directory can create a
symlink to an arbitrary filesystem path; `pydantic-settings` reads the target file and exposes
it as a config value.

`RegStackConfig` extends `BaseSettings` and reads from `secrets_env_path`
(`regstack.secrets.env`). Attack requires write access to the secrets directory (CVSS `AC:H`),
but any deployment storing `regstack.secrets.env` alongside operator-writable files is in scope.

**Action required:**
1. Bump `"pydantic-settings>=2.14.2"` in `pyproject.toml`.
2. Run `uv lock`.

---

## 🟡 INFO (Best practice recommendations)

### I-1 — `UserPublic.tokens_invalidated_after` and `phone_number` exposure (carry-forward)

`UserPublic` exposes `tokens_invalidated_after` (intentional — allows SPA clients to detect
bulk-revocation without an extra round-trip) and `phone_number` (design decision). Hosts with
stricter PII requirements should subclass `UserPublic` or add a projection step. No code change
required in regstack itself; documented here for host awareness.

### I-2 — `admin_promote_pending` path parameter not validated as `EmailStr` (carry-forward)

`POST /admin/pending/{email}/promote` (`src/regstack/routers/admin.py:204`) accepts
`email: str = Path(...)`. A syntactically invalid email address passes parameter binding and
returns a 404 (LookupError path). The endpoint is admin-gated so the risk is low. Fix is a
one-liner: `email: Annotated[EmailStr, Path(...)]`.

### I-3 — Duplicate tracking issue for python-multipart cluster (carry-forward)

Issues #118 (2026-06-23) and #120 (2026-06-24) both track the python-multipart floor bump.
Issue #120 should be updated to enumerate all four CVEs (CVE-2026-53537/53538/53539/53540);
issue #118 should then be closed as a duplicate. This housekeeping task has been outstanding
since the 2026-06-26 review.

---

## 🟢 CLEAN (Passed review)

No code commits landed between 2026-06-29 and 2026-07-01. All sections confirmed clean in the
2026-06-29 review retain that status. The following items were re-verified against the
no-change baseline.

### §1 — Dependency vulnerabilities

| Package | Floor | Resolved (lock) | Status |
|---|---|---|---|
| `fastapi` | `>=0.120.0` | `0.136.1` | ✅ No new CVEs in review window |
| `starlette` | `>=1.0.1` | `1.3.1` | ⚠️ Floor too low — see W-2 (lock is patched; consumers may not be) |
| `pydantic` | `>=2.6` | `2.13.3` | ✅ No new CVEs |
| `pydantic-settings` | `>=2.2` | `2.14.0` | ⚠️ CVE-2026-32690 — lock vulnerable — see W-5 |
| `pyjwt` | `>=2.13.0` | `2.13.0` | ✅ All June-2026 cluster CVEs fixed; no new advisories |
| `cryptography` | `>=46.0.7` (oauth) | `47.0.0` | ⚠️ CVE-2026-45447 — lock vulnerable — see W-3 |
| `jinja2` | `>=3.1.6` | `3.1.6` | ✅ No new CVEs |
| `python-multipart` | `>=0.0.27` | `0.0.28` | ⚠️ Four CVEs — lock vulnerable — see W-1 |
| `pwdlib[argon2]` | `>=0.2.1` | `0.3.0` | ✅ No advisories |
| `sqlalchemy[asyncio]` | `>=2.0` | `2.0.49` | ✅ No new CVEs; no advisories for 2.0.x series |
| `aiosmtplib` | `>=3.0` | `5.1.0` | ⚠️ CVE-2026-53533 SMTP injection — lock vulnerable — see W-4 |
| `pymongo` | `>=4.9` (optional) | `4.17.0` | ✅ No driver CVEs |
| `asyncpg` | `>=0.29` (optional) | `0.31.0` | ✅ No advisories |
| `dnspython` | `>=2.6` | `2.8.0` | ✅ No new CVEs |
| `aioboto3` | `>=12.3` (optional) | `15.5.0` | ✅ No advisories |
| `twilio` | `>=9.0` (optional) | `9.10.5` | ✅ No advisories |
| `slowapi` | `>=0.1.9` (optional) | `0.1.9` | ✅ No advisories |
| `pywebview` | `>=5.0` (optional) | `6.2.1` | ✅ No advisories |

No new CVEs were found in the 2026-06-30 to 2026-07-01 window beyond the five already tracked.

### §2 — Hardcoded secrets

No code changes. No real credentials in `src/`, `tests/`, `examples/`, `docs/`. All `AKIA*`
matches are test-fixture strings wrapped in `SecretStr`. `git ls-files` for credential-file
patterns (`*.env`, `*.key`, `*.pem`, `*secret*`, `*credential*`) returns only
`src/regstack/config/secrets.py` — a utility module containing only `derive_secret` and
`generate_secret` functions; no key material. `git log --all --diff-filter=A` confirms no
credential files were ever committed. `.gitignore` covers `.env`, `regstack.secrets.env`,
`*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.jks`, `*.crt`, `htmlcov/`, `.coverage`. Clean.

### §3 — Authentication & authorization

No code changes. Spot-checks confirmed:
- Every mutating endpoint in `src/regstack/routers/` injects `rs.deps.current_user()` or
  `rs.deps.current_admin()`.
- Bulk-revoke discipline: `update_password` bumps `tokens_invalidated_after`; `update_email`
  bumps `tokens_invalidated_after`; admin `set_active(is_active=False)` calls
  `set_tokens_invalidated_after`; `delete_account` and admin `delete_user` both cascade
  `oauth_identities` before deleting the user row.
- OAuth-only users (`hashed_password=None`) receive the same generic 401 as a wrong password
  on `POST /login`; `change-password`, `change-email`, `delete-account` return 400 pointing
  to the password-reset flow.
- Password comparison via `pwdlib` (Argon2id, constant-time). Clean.

### §4 — Storage-layer injection safety

No code changes. MongoDB repositories use Python dict literals for all query filters. All
`ObjectId()` calls are preceded by `ObjectId.is_valid()`. No `$where`, `$expr`, `$function`,
or `$accumulator` usage. No regex patterns built from user input. SQL repositories use
SQLAlchemy Core parameterised binds throughout. Two `text()` calls exist: `text("SELECT 1")`
(ping) and `sa.text("SELECT COUNT(*) FROM users WHERE hashed_password IS NULL")` (migration
guard); neither interpolates user input. `UtcDateTime.process_result_value()` re-attaches UTC
tzinfo on every read. No unbounded `$in` arrays from user input. Clean.

### §5 — Input validation & injection

No code changes. `extra="forbid"` on all Pydantic request models. `EmailStr` on all
email-bearing request fields. `is_valid_e164()` on phone number input before storage. Jinja2
`autoescape=select_autoescape(["html"])` in `MailComposer`. No `| safe` filter or
`{% autoescape false %}` in any UI or email template. `_validate_redirect()` in
`routers/oauth.py` rejects backslashes, protocol-relative URLs (`//`), and off-origin full
URLs. No shell-command construction in any CLI wizard. Clean.

### §6 — Rate limiting & DoS prevention

No code changes. `LockoutService.check()` fires before password verification in `POST /login`.
MongoDB TTL index on `login_attempts.when` (sized to `login_lockout_window_seconds`).
Admin `/admin/users` listing capped at `limit=200` (`Query(ge=1, le=200)`). OAuth state rows:
TTL index on `expires_at` (Mongo); indexed `expires_at` column (SQL). `oauth_states.result_token`
slot expiry shortened to `completion_ttl_seconds` (default 30 s) after callback completes.
No file-upload endpoints. Clean.

### §7 — Session & token security

No code changes. HS256 with per-purpose derived secrets (`derive_secret(jwt_secret, purpose)`);
non-empty secret enforced at construction (raises `ValueError`). Every token includes `jti`.
`iat` emitted as `float` (microsecond-precision bulk-revoke per `TokenPayload.to_claims()`).
pyjwt `exp`/`iat` checks disabled in `JwtCodec.decode()` in favour of injected `Clock`.
`AuthDependencies._authenticate()` enforces all four gates in order: decode → blacklist jti →
active user → bulk-revoke cutoff. Clean.

### §8 — CSP and the SSR layer

No code changes. Grep confirms zero `<style>` blocks, `style="..."` attributes, or inline
`<script>` tags across all files under `src/regstack/ui/templates/`. `oauth_complete.html`
confirmed stateless (no `{{ user.* }}`). `_token_handoff.html` shared macro uses only
`data-rs-*` attributes. Clean.

### §9 — Data exposure

No code changes. All user-returning endpoints call `UserPublic.from_user()`. JWT decode errors
return a generic 401 in `_authenticate()` (the pyjwt exception text is swallowed; never echoed).
OAuth callback failures redirect with a short code (`?error=<code>`). No `f"... {exc}"` patterns
in HTTP-level error `detail` strings — only in CLI/wizard output (non-HTTP paths). SMTP password,
JWT secret, OAuth `client_secret`, and AWS credentials are all `SecretStr`. Google
`client_secret` reaches `GoogleProvider.__init__` as a raw string but is never logged or included
in responses; only the HTTP status code is logged (at DEBUG) on token exchange failure. Clean.

### §10 — Git history & recent changes

`git log --oneline` since 2026-06-29: one commit — the report file itself (`4ce11d9`). No
changes to `auth/`, `config/secrets.py`, routers, JWT codec, or OAuth subsystem.
`git log --all --diff-filter=A -- '*.env' '*.key' '*.pem' '*secret*' '*credential*'` returns
no new additions. Clean.

### §11 — Third-party integration security

No code changes. SMTP password loaded as `SecretStr`, never logged. AWS credentials loaded
as `SecretStr` from the config chain, never hardcoded. Twilio auth token as `SecretStr`,
lazy import guards base installs. Google `client_secret` never echoed in logs or responses.
JWKS fetch offloaded to `asyncio.to_thread` with `PyJWKClient(timeout=5)` to bound blocking.
The aiosmtplib W-4 advisory applies until the floor is bumped to `>=5.1.1`. Clean (pending
W-4 fix).

### §12 — GitHub Actions workflow security

No workflow changes. Confirmed:
- All `uses:` directives pinned to commit SHAs (not mutable tags):
  `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` (v6.0.2),
  `astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b` (v8.1.0),
  `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (v7.0.1),
  `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1),
  `pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b` (v1.14.0).
- No `pull_request_target` trigger. No self-hosted runners.
- No `${{ github.event.* }}` interpolation in `run:` blocks.
- Workflow-level `permissions: contents: read`; publish job adds only `id-token: write`.
- `attestations: true` (PEP 740 sigstore attestations enabled).
- Cache keyed on `uv.lock` (not user-controlled input).
- `workflow_dispatch:` absent from `publish.yml` (I-10 fix already merged).
- Clean.

### §13 — PyPI package integrity

No code changes or new releases since 2026-06-29. `pyproject.toml` `[tool.hatch.build.targets.wheel]`
packages `src/regstack` only. `[tool.hatch.build.targets.sdist]` excludes `.git`, `.github/`,
`CLAUDE.md`, `docs/`, `examples/`, `scripts/`, `tasks.py`, `tasks/`, `tests/`, `uv.lock`.
Version tag-push trigger only; no `workflow_dispatch`. Publish job uses OIDC trusted publishing
(no `PYPI_API_TOKEN`). Version verification reads from `pyproject.toml` via `tomllib`
(not by importing `regstack.version`). Optional-only extras (`pymongo`, `asyncpg`, `aioboto3`,
`twilio`, `pyjwt[crypto]`) confirmed absent from the base `dependencies` list. The
`tests/unit/test_base_install_imports.py` and the `base-install-smoketest` CI job guard this.
Clean.

### §14 — OAuth subsystem audit

No code changes. Confirmed:
- PKCE: `code_verifier` stored on the `OAuthState` row only; only `code_challenge`
  (SHA-256 of verifier) reaches the provider URL. `_begin_flow()` in `routers/oauth.py`.
- State row lifecycle: `oauth_callback` rejects missing rows (`bad_state`) and expired rows
  using `rs.clock.now()` (not `datetime.now(UTC)`). `/oauth/exchange` uses
  `oauth_states.consume()` for atomic read-and-delete — a second exchange with the same id
  returns 404.
- ID-token verification: `GoogleProvider.verify_id_token` validates signature (via JWKS),
  `iss`, `aud`, `exp`, `nonce`. Failures raise `OAuthIdTokenError` with a generic message;
  the specific reason is logged at WARNING but never echoed in the redirect.
- Linking-policy: `email_in_use` when `auto_link_verified_emails` is off; `auto-link` only
  fires when `email_verified=True`. `identity_in_use` and `already_linked` guards in
  `_resolve_user()`.
- OAuth-only side effects: `POST /login` returns generic 401 for `hashed_password=None`;
  `change-password`, `change-email`, `delete-account` return 400. `DELETE /oauth/{p}/link`
  refuses when no other auth method exists.
- `(provider, subject_id)` and `(user_id, provider)` unique constraints enforced at DB level
  (Mongo: `provider_subject_unique`, `user_provider_unique` indexes; SQL: `UniqueConstraint`).
- Hook events: `oauth_signin_started`, `oauth_signin_completed`, `oauth_account_linked`,
  `oauth_account_unlinked` all present in `KNOWN_EVENTS` and fired at correct points. Clean.

### §15 — Multi-backend parity & schema migration safety

No code changes. Confirmed:
- `test.yml` CI matrix runs `pytest -n auto` with both MongoDB and Postgres services alongside
  SQLite, covering all three backends on every push.
- SQL migrations: `0001_initial_schema.py` and `0002_oauth.py` both have working `upgrade()`
  and `downgrade()` functions. `0002_oauth.py` uses `batch_alter_table` for the
  `hashed_password` nullable column — SQLite-compatible.
- Mongo index parity: `users.email` → `email_unique`; `oauth_identities (provider, subject_id)`
  → `provider_subject_unique`; `oauth_identities (user_id, provider)` → `user_provider_unique`.
  All SQL `UniqueConstraint`s have matching Mongo counterparts.
- TTL handling: Mongo TTL indexes on `blacklist.exp`, `pending.expires_at`,
  `login_attempts.when`, `mfa_codes.expires_at`, `oauth_states.expires_at`. SQL backends rely
  on `purge_expired()` reapers (documented in each repo's method).
- `tz_aware=True` on the Mongo client; `UtcDateTime` TypeDecorator on all SQL datetime
  columns. No drift. Clean.

---

## 📊 Summary

| Item | Value |
|---|---|
| Date of review | 2026-07-01 |
| Version reviewed | 0.8.4 |
| Commits since last review | 1 (report only — no code changes) |
| 🔴 CRITICAL | 0 |
| 🟠 WARNING | 5 (all carry-forward, unresolved since 2026-06-23 – 2026-06-26) |
| 🟡 INFO | 3 (all carry-forward) |
| New findings this review | 0 |
| Previous review | 2026-06-29 (5 warnings, 0 critical) |

**Comparison with previous review:** Identical finding set. Five warnings and three info items
have been open for 5–8 days without resolution. No code changes landed on `main` in this
interval.

**Top 3 priorities for the development team:**

1. **W-4 (SMTP injection, `aiosmtplib>=5.1.1`)** — The locked version (`5.1.0`) is the
   vulnerable version on the email delivery paths for verification, password-reset, and MFA
   tokens. One-line floor bump + `uv lock`.

2. **W-1 (python-multipart DoS cluster, `>=0.0.32`)** — CVE-2026-53539 (CVSS 7.5, CPU DoS) is
   unauthenticated and directly reachable via any form-encoded endpoint. The locked version
   (0.0.28) is in the vulnerable range.

3. **W-5 (pydantic-settings symlink traversal, `>=2.14.2`)** — The locked version (`2.14.0`)
   is the vulnerable version for the secrets-directory path traversal. Low-likelihood but the
   fix is a one-line bump.

W-2 (starlette) and W-3 (cryptography) are lower urgency because the *locked* version is
already patched in both cases — the risk is entirely downstream consumer-facing.
