# Security Review — 2026-07-30

**Reviewer:** Automated security review agent
**Repository:** jdrumgoole/regstack
**Date:** 2026-07-30
**Version:** 0.9.1
**Sections covered:** 1–15 (all)

---

## 🔴 CRITICAL (Immediate action required)

None identified.

---

## 🟠 WARNING (Address within 1 week)

None identified.

---

## 🟡 INFO (Best practice recommendations)

### I1 — Per-IP rate limiting default-off *(carry-over)*

**File:** `docs/configuration.md:248–258`, `src/regstack/config/schema.py:224–241`

Per-IP rate limits on `/register`, `/forgot-password`, and `/resend-verification` default to unset. The
documentation explains why (enabling them fails closed for hosts without the `rate_limit` extra / `slowapi`);
per-account lockout on `/login` is always on. Operators should set IP-level limits appropriate to their
traffic profile.

**Recommendation:** Configure `register_rate_limit`, `forgot_password_rate_limit`, and
`resend_verification_rate_limit` in production deployments to protect outbound email budget and
prevent address-harvesting probes.

---

### I2 — `UserPublic.tokens_invalidated_after` exposed in API responses *(carry-over)*

**File:** `src/regstack/models/user.py:105`

`tokens_invalidated_after` appears in `/me`, `/admin/users/{id}`, and every `UserPublic` response.
Documented trade-off: SPAs use it to detect stale sessions. If internal event timing is sensitive,
the field should be excluded from `UserPublic`. No change since last review.

---

### I3 — Admin error messages echo the `email` path parameter *(carry-over)*

**File:** `src/regstack/routers/admin.py:224,229`

Two admin-only endpoints include the email from the URL path in error responses. Safe today because
only superusers reach these endpoints. If the admin guard were ever accidentally removed, these
messages would leak whether an address exists — contradicting the anti-enumeration discipline on
`/forgot-password` and `/resend-verification`. No change since last review.

---

### I4 — `oauth` extra explicitly pins `cryptography>=48.0.1` *(carry-over)*

**File:** `pyproject.toml:98`

The `oauth` extra carries an explicit `cryptography>=48.0.1` floor alongside `pyjwt[crypto]>=2.13.0`
because `pyjwt[crypto]` transitively pulls `cryptography` without a floor high enough to close
CVE-2026-45447 (CVSS 9.8 Critical — heap use-after-free in OpenSSL `PKCS7_verify()`, first clean
release 48.0.1). The explicit pin is correct defence-in-depth. No change since last review.

---

### I5 — SNS SMS backend uses boto3 default credential chain *(carry-over)*

**File:** `src/regstack/sms/sns.py:29`

`aioboto3.Session()` is constructed with no credential arguments and relies on the ambient boto3
credential chain (instance profile, env vars, etc.). Asymmetric with the SES email backend which
exposes explicit `ses_access_key_id` / `ses_secret_access_key` config fields. Noted for operator
awareness. No change since last review.

---

### I6 — OAuth exchange consumes expired state before returning 400 *(carry-over)*

**File:** `src/regstack/routers/oauth.py:151–161`

`/oauth/exchange` atomically consumes (deletes) the state row and then checks `state.expires_at
< rs.clock.now()`. An expired row is permanently consumed before the 400 is returned, forcing the
user to restart the OAuth flow. The ordering is secure (no replay possible after delete), but
check-before-consume would be friendlier. No change since last review.

---

### I7 — `cmd:` log-source kind executes operator-supplied shell commands *(carry-over)*

**File:** `src/regstack/cli/validate/logtail.py:106`

The `cmd:<shell>` log-source kind runs `sh -c <target>` where `<target>` is the operator's
configured value. This is the documented contract for this source kind — intentional operator-level
behaviour, not user input, and not reachable through the HTTP API. Noted for auditors. No change
since last review.

---

### I8 — `SecantusDB>=0.6.0b2` is a pre-release dependency in the `secantus` extra *(new)*

**File:** `pyproject.toml:94`; `uv.lock` (locked at `secantusdb==0.6.0b3`)

The `secantus` extra (`pip install regstack[secantus]`) pulls in `SecantusDB>=0.6.0b2`. The current
`uv.lock` pins `0.6.0b3` (uploaded 2026-07-28); `0.6.0b4` was released 2026-07-29 and is not yet
in the lock file. No CVEs have been found against any SecantusDB release.

Three notes:

1. **Pre-release maturity.** Beta versions receive less scrutiny from the broader security community
   than stable releases. `SecantusDB` is a relatively new package (first release in this beta
   series) and does not yet appear in any CVE database, which means both "no known issues" and
   "no issues yet discovered." The package bundles WiredTiger C extensions (~15–17 MB per wheel)
   whose security track record is distinct from the Python wrapper.

2. **Lock-file drift.** The `uv.lock` pins `0.6.0b3` but the latest available is `0.6.0b4`. CI
   installs the pinned version (safe), but hosts using `pip install regstack[secantus]` without a
   lock file will receive `0.6.0b4`. The next `uv lock` run should pick it up.

3. **Scope.** `SecantusDB` is never imported at module scope in `src/regstack/` — the doctor's
   `buildInfo` detection only reads a response field. Hosts that never use the `secantus` extra are
   completely unaffected. The extra is explicitly documented as "for embedding the server
   in-process."

**Recommendation:** Run `uv lock` to pull in `0.6.0b4`. Consider adding a `regstack[secantus]`
installation path to `tests/unit/test_base_install_imports.py` to verify the extra is importable
once a stable SecantusDB release is available. Promote the floor from the beta specifier to a
stable release floor once SecantusDB ships 1.0.0.

---

## 🟢 CLEAN (Passed review)

**§1 — Dependency CVEs (window: 2026-05-01 – 2026-07-30):**
Scan of all 21 base + optional-extra packages found no new advisories requiring floor bumps.
Every 2026 CVE in scope is already closed by the pinned floors documented in `pyproject.toml`:
starlette four-CVE cluster (BadHost CVE-2026-48710 through CVE-2026-54283), pyjwt six-CVE
cluster (CVE-2026-48522 through CVE-2026-48526 + CVE-2026-32597), pydantic-settings two-CVE
pair (CVE-2026-32690 / CVE-2026-58203), python-multipart seven CVEs (CVE-2026-40347,
CVE-2026-42561, CVE-2026-53537–53540, CVE-2026-24486), aiosmtplib SMTP injection
(CVE-2026-53533), jinja2 sandbox breakout (CVE-2025-27516), and the cryptography four-CVE
cluster including the CVSS 9.8 Critical CVE-2026-45447.
No packages yanked. No package more than 2 major versions behind its latest release.
The `oauth` extra correctly declares `pyjwt[crypto]`, `cryptography>=48.0.1`, and (as of 0.9.1)
`httpx>=0.27` — no extraneous transitive pull-ins.

**§2 — Hardcoded secrets:** No real credentials in `src/`, `tests/`, `examples/`, or `docs/`.
`.gitignore` covers `regstack.secrets.env`, `.env`, `.env.*`, `*.pem`, `*.key`, `*.p12`,
`*.pfx`, `htmlcov/`, `.coverage`. The only file matching sensitive filename patterns in
`git ls-files` is `src/regstack/config/secrets.py` (the `derive_secret` helper — expected).
No sensitive files in git history (checked with `--diff-filter=A` over credential-name globs).

**§3 — Auth/authz:** All endpoints correctly inject `current_user` or `current_admin`. No
endpoint returns `BaseUser` directly — every public-facing response goes through
`UserPublic.from_user()`. Bulk-revoke discipline intact across all flows:
password-change bumps `tokens_invalidated_after`; reset-password calls `update_password`
AND `lockout.clear`; email-change confirm bumps `tokens_invalidated_after`; admin disable
(`is_active=False`) calls `set_tokens_invalidated_after`. Account deletion cascades
`oauth_identities` before the user row. OAuth-only users (`hashed_password=None`) receive
the same generic 401 on password login — no enumeration. `change-password`/`change-email`/
`delete-account` return 400 for OAuth-only users. Password comparison uses pwdlib/Argon2id
constant-time verify (not `==`). JWT keys derived per-purpose via `derive_secret`.

**§4 — Storage injection:** MongoDB repositories use literal string keys in all query
filters. No `$where`, `$expr`, `$function`, or `$accumulator` usage found. `ObjectId.is_valid()`
guards URL path parameters before query. No user-input regex patterns. SQL repositories use
SQLAlchemy parameterised expressions throughout; no f-string or `%`-interpolation into raw SQL.
The two `text()` calls are `text("SELECT 1")` (health-check ping) and a hardcoded migration count
query — neither interpolates user input. `UtcDateTime` TypeDecorator re-attaches UTC on read in
`process_result_value`. All `purge_expired()` implementations use SQLAlchemy bound parameters for
the cutoff timestamp.

**§5 — Input validation:** All request models use Pydantic with `extra="forbid"`. `EmailStr`
on every email-bearing field. `is_valid_e164()` called before phone number storage. Jinja2
autoescape enabled for HTML in both email and SSR Jinja environments. No `| safe` filters
or `{% autoescape false %}` blocks found in any template. OAuth `redirect_to` validated
same-origin with backslash rejection and double-slash guard via `_validate_redirect`.
Provider-supplied `error` query params sanitised with `_sanitize_for_log()`. No user input
reaches a shell command.

**§6 — Rate limiting:** `LockoutService.check()` runs before password work;
`record_failure()` runs before the `is_active`/`is_verified` branches. Mongo TTL index on
`login_attempts.when` sized to `login_lockout_window_seconds`. Admin user listing caps
`limit` at 200. OAuth state rows TTL on both backends. No file-upload endpoints.

**§7 — JWT/session:** Algorithm locked to the configured Literal type (HS256/384/512).
Every JWT carries a `jti` (`secrets.token_urlsafe(16)`). `iat` emitted as a float via
`self.iat.timestamp()` — correct for float-precision bulk-revoke. pyjwt `verify_exp`,
`verify_iat`, and `verify_nbf` all disabled; expiry manually checked against the injected
`Clock` (`now >= exp`). Revocation chain in `AuthDependencies._authenticate`:
(1) blacklist `is_revoked(jti)`, (2) user existence + `is_active`, (3) `is_payload_bulk_revoked`.
No token values in any log calls. JWT secret minimum-length guard (`MIN_JWT_SECRET_LENGTH = 32`)
enforced at `JwtCodec.__init__` construction time.

**§8 — CSP/SSR:** SSR templates (`src/regstack/ui/templates/`) load `regstack.js` via
`<script src defer>` — no inline scripts. No `<style>` blocks or `style=` attributes in any
SSR template (email templates contain inline styles as expected for email clients; this is
outside the scope of web CSP). `oauth_complete.html` follows the same discipline. SSR pages
are stateless — no auth state rendered into HTML.

**§9 — Data exposure:** All API responses use `UserPublic.from_user()`. DB errors return
generic 500; JWT decode errors return the static `"Invalid or expired token."` string; OAuth
failures redirect with short opaque codes (`bad_state`, `state_expired`, `email_in_use`,
etc.) — no exception text echoed. JWT secret, OAuth `client_secret`, and SMTP password are
all `SecretStr` and do not appear in any log call. OAuth token-exchange failure logs only
the HTTP status code (`DEBUG` level), not the response body.

**§10 — Git history (since 2026-07-29 review):** Four commits since the last review:
`feat: support SecantusDB as a production deployment target` (0.9.0), `fix(wizard): shutdown
flag must not be loop-bound` (wizard stability fix, not security-relevant),
`fix(docs): repair changelog heading levels` (docs only), `test(packaging): assert sdist
contents against an allowlist` (packaging quality improvement), and
`Declare httpx in the oauth extra (#157)` (0.9.1 bug-fix: adds `httpx>=0.27` to the `oauth`
extra to close `ModuleNotFoundError` on OAuth-enabled deploys without the `dev` extra).
No changes to core auth paths, the JWT codec, OAuth ID-token verification, or the storage
repositories. No credential files in git history (verified with `--diff-filter=A` over
credential-name globs).

**§11 — Third-party integrations:** SMTP `smtp_password` is `SecretStr`; never echoed in
logs or error messages. SES `access_key_id` / `secret_access_key` are `SecretStr`. Twilio
`twilio_auth_token` is `SecretStr`. Google `client_secret` passed as `.get_secret_value()`
to the token endpoint, never logged. `PyJWKClient` constructed with `timeout=5`; JWKS fetch
offloaded to `asyncio.to_thread`. ID-token verification requires and validates `sub`, `iss`,
`aud`, `exp`, `iat`, `nonce`; algorithm pinned to `["RS256"]`; nonce checked separately
after decode. No credentials hardcoded in any backend.

**§12 — GitHub Actions:** Two workflows: `test.yml` and `publish.yml`. All third-party
actions are SHA-pinned (not mutable version tags):
`actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` (# v6.0.2),
`astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b` (# v8.1.0),
`actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (# v7.0.1),
`actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (# v8.0.1),
`pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b` (# v1.14.0).
No `pull_request_target` trigger. No self-hosted runners. No `${{ github.event.* }}`
interpolation in any `run:` step. `publish.yml` defaults to `permissions: { contents: read }`
at workflow level; the `publish` job adds only `id-token: write` for OIDC plus `contents:
read`. No `PYPI_API_TOKEN` secret. PEP 740 sigstore attestations enabled. `workflow_dispatch`
is correctly absent from `publish.yml`. Tag-version verification reads from `pyproject.toml`
via `tomllib`, not by importing `regstack`. Test suite updated label to "all four backends"
(comment + step name change only, no functional change).

**§13 — PyPI package integrity:** `[tool.hatch.build.targets.wheel]` ships `src/regstack/`
only. Sdist `exclude` list covers `docs/`, `tests/`, `examples/`, `scripts/`, `tasks/`,
`tasks.py`, `CLAUDE.md`, `.github/`, `.git`, `uv.lock`. Optional backend drivers
(`pymongo`, `asyncpg`, `aioboto3`, `twilio`, `pyjwt[crypto]`, `SecantusDB`) are all in
`optional-dependencies`, not in the base `dependencies` list.
`tests/unit/test_base_install_imports.py` + the `base-install-smoketest` CI job verify this
continuously. `tests/unit/test_extra_declares_its_imports.py` (new in 0.9.1) enforces that
every module-scope import in extra-gated subpackages (`oauth`, `backends/mongo`) is declared
in the corresponding extra — the test that caught the `httpx` omission and its specific
regression case. Version `0.9.1` is consistent between `pyproject.toml` and
`src/regstack/version.py`. OIDC trusted publishing via `pypa/gh-action-pypi-publish`; no
long-lived `PYPI_API_TOKEN` secret. Sigstore PEP 740 attestations enabled.

**§14 — OAuth subsystem:** `code_verifier` stored on the state row; only `code_challenge`
(SHA-256 base64url) goes to the provider redirect. State callback uses `rs.clock.now()` for
expiry (not `datetime.now(UTC)`). `/oauth/exchange` atomically consumes the row before checking
expiry (see I6 for the consume-before-check ordering note). ID-token verification validates
algorithm (`RS256`), audience (client_id), issuer, required claims, expiry, and nonce.
Linking policy: `email_in_use` for existing email-only accounts unless
`oauth.auto_link_verified_emails=True` AND `email_verified=true`; `identity_in_use` and
`already_linked` enforced correctly. OAuth-only users receive generic 401 on password login;
correct 400 on `change-password`/`change-email`/`delete-account`; `DELETE /oauth/{provider}/link`
returns 400 when the provider is the only sign-in method. `(provider, subject_id)` and
`(user_id, provider)` uniqueness enforced at DB level (Mongo unique indexes, SQL
`UniqueConstraint`). OAuth hooks fire at the documented event names.

**§15 — Multi-backend parity:** CI matrix (`test.yml`) runs `pytest -n auto` against SQLite,
PostgreSQL (via service container), MongoDB (via service container), and SecantusDB (embedded
in-process, no container needed — added automatically when `uv sync --extra dev` installs the
package). The SecantusDB parametrization was verified against the full suite: 655 passed / 24
skipped, where the skips are Mongo-specific tests (TTL indexes, unique-constraint surfacing,
`$jsonSchema` validation, ObjectId guards) correctly marked to skip on non-native-mongod
backends. SQL migrations have both `upgrade()` and `downgrade()`; column alterations use
`batch_alter_table` for SQLite compatibility. Mongo unique indexes match SQL `UniqueConstraint`
declarations (`users.email_unique`, `oauth_identities (provider, subject_id)`,
`oauth_identities (user_id, provider)`). `UtcDateTime` TypeDecorator and `tz_aware=True`
Mongo client enforce UTC-aware datetimes throughout.

---

## 📊 Summary

- **Date of review:** 2026-07-30
- **regstack version reviewed:** 0.9.1
- **Critical findings:** 0
- **Warning findings:** 0
- **Info findings:** 8 (7 carry-overs unchanged + 1 new: SecantusDB pre-release in `secantus` extra)
- **Sections reviewed:** 1–15 (all)
- **Comparison with previous review (2026-07-29 at v0.8.6):** The two releases since
  the last review (0.9.0 SecantusDB support, 0.9.1 httpx-in-oauth fix) introduced no
  regressions in any previously-clean area. The `httpx>=0.27` addition to the `oauth`
  extra correctly closes a `ModuleNotFoundError` on OAuth-enabled deployments without the
  `dev` extras. SecantusDB support reuses the existing Mongo backend with no new auth or
  storage code paths. No new CVEs require floor bumps in the 2026-05-01–2026-07-30 window.

**Top 3 priorities for the development team:**

1. **[I8] Track SecantusDB towards a stable release floor.** The `secantus` extra pins a
   beta version (`>=0.6.0b2`). Run `uv lock` to pull in `0.6.0b4` (released 2026-07-29).
   Once SecantusDB ships a stable `1.0.0`, tighten the floor from `>=0.6.0b2` to `>=1.0.0`
   and add it to the `test_extra_declares_its_imports.py` gated-package checks.

2. **[I1] Configure per-IP rate limits in deployment.** A fresh regstack deployment has
   per-account lockout on `/login` but no IP-level rate limiting on `/register`,
   `/forgot-password`, or `/resend-verification`. Operators should set limits appropriate
   to their traffic profile using the `rate_limit` extra and the `*_rate_limit` config fields.

3. **[I2] Decide whether `tokens_invalidated_after` belongs in `UserPublic`.** The timestamp
   leaks when sessions were last force-invalidated. For most deployments this is a useful SPA
   optimisation; for higher-sensitivity deployments it is internal metadata that should be
   excluded. Document the reasoning in `UserPublic` so the decision is explicit.
