"""Concrete OAuth providers.

v1 ships only :class:`~regstack.oauth.providers.google.GoogleProvider`.
Adding a provider is a new module here plus one entry in
:class:`~regstack.oauth.registry.OAuthRegistry`.
"""
