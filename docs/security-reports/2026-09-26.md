# Security Review — 2026-09-26

**Repo:** jdrumgoole/regstack
**HEAD reviewed:** `c3ae87da6e4b9e5328b800f83d8652997bf552c6`
**Version:** 0.9.3
**Reviewer:** automated daily scan
**Previous report:** [2026-09-25](./2026-09-25.md)

---

## Summary

| Severity | New | Carry-forward | Resolved |
|----------|-----|---------------|----------|
| 🔴 CRITICAL | 0 | 0 | 0 |
| 🟠 WARNING | 0 | 9 | 0 |
| 🟡 INFO | 0 | 2 | 0 |
| 🟢 CLEAN | — | — | — |

No product-code changes since `2026-09-09` (17 days). All nine WARNINGs and two INFOs carry forward unchanged. All nine WARNINGs are past their 7-day remediation SLA; remediation velocity remains zero.

---

## Section 1 — Dependency CVEs

### 🟠 W-1 — `cryptography` floor below patched release (day 47 — **32 days past SLA**)

`pyproject.toml` declares `cryptography>=48.0.1` in both the `oauth` extra and the `dev` extra. The minimum safe floor is **50.0.1**, covering five CVEs:

- **CVE-2026-0001** (RSA PKCS#1 v1.5 timing side-channel — fixed in 50.0.0)
- **CVE-2026-0002** (X.509 parsing OOM — fixed in 50.0.0)
- **CVE-2026-69247** (PKCS#7 `EnvelopedData` Bleichenbacher oracle — CVSS moderate; fixed in 50.0.1)
- **CVE-2026-69248** (wildcard DNS name allows escape from `permittedSubtrees` — fixed in 50.0.1)
- **CVE-2026-69249** (duplicate self-signed intermediates cause exponential path-building — DoS; fixed in 50.0.1)

The `pyjwt[crypto]` OAuth path loads RSA and X.509 primitives at import time; all five CVEs are on the live code path for OAuth-enabled deployments. Oracle Linux security advisories ELSA-2026-64774 and ELSA-2026-64795 confirm active distribution of patches for 69248 and 69249 this month.

**Remediation:** Bump floor to `cryptography>=50.0.1` in both entries in `pyproject.toml`; run `uv lock` and push.

### 🟢 CLEAN — Other major dependencies

`fastapi>=0.120.0`, `starlette>=1.3.1`, `pydantic-settings>=2.14.2`, `pyjwt>=2.13.0`, `jinja2>=3.1.6`, `python-multipart>=0.0.32`, `pymongo>=4.9`, `asyncpg>=0.29`, `sqlalchemy[asyncio]>=2.0`, `aiosqlite>=0.20`, `pwdlib[argon2]>=0.2.1`, `click>=8.1`, `dnspython>=2.6` all have floors covering known CVEs. Starlette BadHost (CVE-2026-48710), the Starlette four-CVE cluster (48818, 54282, 54283), and the PyJWT cluster (CVE-2026-32597, 48522–48526) are all covered by existing floors. No new September 2026 CVEs found for these packages beyond those already documented.

### 🟠 W-5 — `aiosmtplib` floor below patched release (day 20 — **13 days past SLA**)

`pyproject.toml` declares `aiosmtplib>=5.1.1`. CVE-2026-0010 (moderate; STARTTLS stripping when the remote drops `EHLO` capabilities mid-handshake, fixed in 5.1.2) remains unaddressed. Any deployment using `email_backend=smtp` with STARTTLS is exposed.

**Remediation:** Bump floor to `aiosmtplib>=5.1.2` in `pyproject.toml`; run `uv lock`.

### 🟢 CLEAN — Extra-hygiene check

`oauth` extra: `pyjwt[crypto]>=2.13.0`, `cryptography>=48.0.1`, `httpx>=0.27` only. `postgres` extra: `asyncpg>=0.29` only. Neither pulls unexpected transitive extras. `pymongo`, `asyncpg`, `aioboto3`, `twilio`, and `pyjwt[crypto]` are in `optional-dependencies`, not the base `dependencies` list.

---

## Section 2 — Hardcoded Secrets Scan

### 🟢 CLEAN — Source tree

No real secrets, private keys, JWTs, or connection strings with embedded credentials found in `src/`, `tests/`, `examples/`, or `docs/`. The `AKIAEXAMPLE` pattern in `tests/unit/test_ses_backend.py` is a clearly labelled placeholder. Test JWT fragments (`eyJ…`) appear only as test fixtures with invalid signatures. `src/regstack/config/secrets.py` is a Python module (key-derivation logic), not a secrets file.

### 🟢 CLEAN — Git history

`git log --all --diff-filter=A -- '*.env' '*.key' '*.pem' '*secret*' '*credential*'` returned only the `release: 0.9.3` commit for `src/regstack/config/secrets.py` (the Python module). All commits since 2026-09-09 are docs-only security review reports. No sensitive files are tracked.

### 🟢 CLEAN — `.gitignore`

`.gitignore` covers `.env`, `.env.*`, `regstack.secrets.env`, `**/regstack.secrets.env`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.jks`, `*.crt`, `.coverage`, `htmlcov/`, `.idea/`, `.vscode/`. Coverage is adequate; no gaps identified.

---

## Section 3 — Authentication & Authorisation Audit

### 🟢 CLEAN — Endpoint authentication coverage

All mutating endpoints inject `rs.deps.current_user()` or `rs.deps.current_admin()`. Admin endpoints (`/admin/stats`, `/admin/users`, `/admin/users/{id}`, `PATCH /admin/users/{id}`, `DELETE /admin/users/{id}`, `/admin/users/{id}/resend-verification`, `/admin/pending/{email}/promote`) all depend on `rs.deps.current_admin()`. All data responses use `UserPublic.from_user()`.

### 🟢 CLEAN — Bulk-revoke discipline

Password reset calls `users.update_password()` (which atomically bumps `tokens_invalidated_after`) and `lockout.clear()`. Password change calls `users.update_password()` and `lockout.clear()`. Admin disable calls `users.set_tokens_invalidated_after()` before returning 200. Account deletion cascades OAuth identities before deleting the user row. Email-change confirm calls `users.update_email()` which atomically bumps `tokens_invalidated_after`. All pass the discipline check (modulo W-2 and W-3 below, which concern the token lifecycle around email-change, not the revocation bump itself).

### 🟠 W-8 — Timing oracle on login for OAuth-only accounts (day 8 — **1 day past SLA**)

`src/regstack/routers/login.py:99`: the login handler contains:

```python
if user.hashed_password is None or not rs.password_hasher.verify(
    payload.password, user.hashed_password
):
```

Python short-circuits on `hashed_password is None`: if the account is OAuth-only, the ~100 ms Argon2 verify is skipped and the response returns in ~1 ms. An attacker can probe whether a given email is OAuth-only vs password-enabled with ~200 requests and a simple latency histogram.

**Remediation:** Always run a dummy Argon2 verify when `hashed_password is None` to preserve constant time (e.g., `rs.password_hasher.dummy_verify()` or equivalent constant-time guard), then raise the same generic 401.

### 🟢 CLEAN — `hashed_password is None` side effects

`change-password`, `change-email`, `delete-account` all call `require_password_set(user)` before proceeding. OAuth unlink refuses if it would remove the only sign-in method. Login returns the same generic 401 for missing password as for a wrong password (modulo the timing gap above, W-8).

### 🟢 CLEAN — Constant-time password comparison

`pwdlib` (argon2) is used for all password hashing and verification. No `==` comparison is used on raw password strings.

### 🟢 CLEAN — Per-purpose JWT key derivation

`derive_secret(jwt_secret, purpose)` in `src/regstack/config/secrets.py` uses `hmac.new(master, purpose.encode(), hashlib.sha256).digest()`. Each purpose (`session`, `password_reset`, `email_change`, `phone_setup`, `login_mfa`) gets a distinct derived key. Compromise of one derived key does not compromise the master.

---

## Section 4 — Storage-layer Injection Safety

### 🟢 CLEAN — MongoDB backend

All MongoDB queries use typed BSON document constructors with literal operator names. No user input is interpolated into query operators. No `$where`, `$expr`, `$function`, or `$accumulator` operators are used. No regex patterns are built from user input. `get_by_id` guards with `ObjectId.is_valid(user_id)` before casting throughout all Mongo repos. No `$in` queries accept unbounded user-supplied arrays.

### 🟢 CLEAN — SQL backend

All SQL queries in `src/regstack/backends/sql/repositories/` use SQLAlchemy Core with parameterised binds. No f-string or `%`-interpolation into raw SQL. Two `text(...)` usages exist: `text("SELECT 1")` for a health-ping (no user input); `text("SELECT COUNT(*) FROM users WHERE hashed_password IS NULL")` in migration `0002_oauth.py` (no user input). `purge_expired()` implementations across all five SQL repos use bound `now` parameters throughout.

### 🟢 CLEAN — `UtcDateTime` TypeDecorator

`src/regstack/backends/sql/types.py` consistently re-attaches UTC tzinfo on result values via `process_result_value`. Type error on non-datetime values is raised rather than silently passed.

### 🟠 W-6 — SQL `OAuthStateRepo.consume` non-atomic read-delete (day 18 — **11 days past SLA**)

`src/regstack/backends/sql/repositories/oauth_state_repo.py`: `consume` performs a `SELECT` followed by a `DELETE` in a single transaction. Under both PostgreSQL (READ COMMITTED) and SQLite, two concurrent connections can each execute the SELECT and observe the row before either commits the DELETE, allowing both to receive the same state row and exchange the OAuth token twice.

The MongoDB backend uses `find_one_and_delete`, which is atomic at the server level. The SQL path must match this guarantee.

**Remediation:** Rewrite `consume` to use `DELETE … RETURNING *` (PostgreSQL) or an equivalent serialised exclusive transaction pattern (SQLite) so the SELECT and DELETE are one atomic server-side operation.

---

## Section 5 — Input Validation & Injection

### 🟢 CLEAN — Pydantic input models

Every endpoint accepts a typed Pydantic model with `extra="forbid"`. No endpoint accepts a raw `dict` or untyped JSON body.

### 🟢 CLEAN — Email and phone validation

All email-bearing inputs use `EmailStr`. `is_valid_e164()` is called on phone number input before storage.

### 🟢 CLEAN — Email and SSR templates

`MailComposer._build_env()` constructs the Jinja2 `Environment` with `autoescape=select_autoescape(["html"])`. No `| safe` filters or `{% autoescape false %}` blocks found in email templates or SSR templates under `src/regstack/ui/templates/`. No inline `<style>` blocks or `style="…"` attributes in any SSR template (email templates contain `style=` attributes, which is expected for email client compatibility and is not a CSP concern for SSR pages).

### 🟢 CLEAN — CLI wizard and OAuth redirect validation

`src/regstack/cli/init.py` uses `click.prompt` with no shell invocations; no user input is fed into a shell command. `_validate_redirect()` in `src/regstack/routers/oauth.py` rejects backslash-prefixed paths, protocol-relative URLs (`//host`), and cross-origin full URLs on both `/{provider}/start` and `/{provider}/link/start` endpoints.

---

## Section 6 — Rate Limiting & DoS Prevention

### 🟢 CLEAN — Login lockout

`LockoutService.check()` is called before password verification on the login route. `record_failure()` is called before the `is_active`/`is_verified` branches. Successful logins call `lockout.clear()`.

### 🟢 CLEAN — TTL indexes and pagination

Mongo TTL index on `login_attempts` sized to `config.login_lockout_window_seconds`. `/admin/users` enforces `limit: Annotated[int, Query(ge=1, le=200)] = 50`. OAuth state TTL at 5 min default enforced via Mongo TTL index and SQL read-side filtering. No user-facing file upload endpoints found.

---

## Section 7 — Session & Token Security

### 🟢 CLEAN — JWT configuration, `jti`, `iat` float, clock injection, revocation discipline

`JwtCodec.__init__` enforces `len(jwt_secret) >= MIN_JWT_SECRET_LENGTH (32)`. Every token minted by `JwtCodec.encode()` receives `jti=secrets.token_urlsafe(16)`. `iat` is emitted as a float (`payload.iat.timestamp()`). pyjwt's built-in `exp`/`iat` validation is disabled via `options={"verify_exp": False, "verify_iat": False}`; both are validated against the injected `Clock`. `_authenticate` enforces decode → blacklist check → bulk-revoke check → `is_active` in the correct order.

### 🟢 CLEAN — No token leakage in logs

`_authenticate` catches `TokenError` and raises a static 401 without echoing the pyjwt exception text. OAuth callback errors log only generic short codes — never the raw token, id_token, or JWT secret content. OAuth token-exchange response bodies are logged at DEBUG only (not WARNING/ERROR) per the 2026-05-22 review.

### 🟠 W-2 — Email-change token `jti` never blacklisted (day 26 — **19 days past SLA**)

`src/regstack/routers/account.py` → `confirm_email_change`: after successfully decoding the email-change JWT and calling `users.update_email(...)`, the handler never extracts the token's `jti` or calls `blacklist.revoke(jti, exp)`. A valid email-change link can therefore be replayed until it expires (default 60 min TTL), allowing an attacker who obtains a copy of the link to repeatedly swap the email address.

**Remediation:** After `_decode_email_change_token(token)`, extract `payload.jti` and `payload.exp`. Check `await rs.blacklist.is_revoked(jti)` first (→ 400 if already consumed). On success, call `await rs.blacklist.revoke(jti, exp)` before returning.

### 🟠 W-3 — Email-change handler skips bulk-revocation check (day 26 — **19 days past SLA**)

`src/regstack/routers/account.py` → `confirm_email_change`: after `_decode_email_change_token`, the handler fetches the user by `sub` but never calls `is_payload_bulk_revoked(payload, user.tokens_invalidated_after)`. A token minted before a `change-password` event (which bumps `tokens_invalidated_after`) would still be accepted to change the email.

**Remediation:** After `users.get_by_id(user_id)`, add:
```python
if is_payload_bulk_revoked(payload, user.tokens_invalidated_after):
    raise HTTPException(status_code=400, detail="Email-change link has been invalidated.")
```

---

## Section 8 — CSP and the SSR Layer

### 🟢 CLEAN

`src/regstack/ui/templates/base.html` and all child templates contain no inline `<style>` blocks, no `style="…"` attributes, and no inline `<script>` blocks. The `oauth_complete.html` template follows the same discipline. All SSR templates are CSP-friendly — `style-src 'self'` works without `unsafe-inline`. Templates render no auth state into HTML; no `{{ user.email }}` or similar patterns found.

---

## Section 9 — Data Exposure

### 🟢 CLEAN — `UserPublic` projection and error messages

`UserPublic.from_user()` excludes `hashed_password`. All admin and user-facing endpoints return `UserPublic`, never `BaseUser` directly. Authentication failures return a static 401. OAuth callback failures redirect to `?error=<short_code>` only. SMTP/SES/OAuth credentials are accessed only via `.get_secret_value()` and never logged. No `f"...{exc}"` patterns found in HTTP response bodies.

### 🟡 I-1 — `tokens_invalidated_after` exposed in `UserPublic` (day 47)

`src/regstack/models/user.py:105`: `UserPublic` includes `tokens_invalidated_after` in its JSON response. While the field carries a comment noting its use for SPA client-side session-invalidation detection, it is an internal session-management timestamp. Exposing it slightly aids timing analysis of bulk-revocation events and was called out as a gap in the original security-review spec (`§9: UserPublic excludes … tokens_invalidated_after`).

**Status:** Open. No change since first flagged. Low severity — no direct exploit path.

### 🟡 I-3 — Duplicate tracking of `tokens_invalidated_after` in `UserPublic` (day 47)

Same underlying issue as I-1, tracked separately because resolving it requires a minor-version bump and changelog entry to avoid breaking SPAs that rely on the field.

**Status:** Open. No change since first flagged.

---

## Section 10 — Git History & Recent Changes

### 🟢 CLEAN

`git log --oneline -30` shows only security-review report commits since `2026-09-09` (17 days). No product-code changes. No changes touching `auth/`, `config/secrets.py`, the OAuth subsystem, routers, or the JWT codec. No secrets, `.env`, `.pem`, or `.key` files tracked in git history.

---

## Section 11 — Third-Party Integration Security

### 🟢 CLEAN — SMTP backend

`SmtpEmailService` reads `smtp_password` only via `self._config.smtp_password.get_secret_value()` at send time. Password never appears in logs or error messages.

### 🟢 CLEAN — SES backend

`EmailConfig` declares `ses_access_key_id: SecretStr | None` and `ses_secret_access_key: SecretStr | None`. Credentials are loaded from config or ambient credential chain; never hardcoded. `aioboto3` import is lazy.

### 🟠 W-9 — `SmsConfig` SNS credential fields not `SecretStr` (day 8 — **1 day past SLA**)

`src/regstack/config/schema.py`: `SmsConfig` declares `twilio_auth_token: SecretStr | None` correctly for the Twilio path, but the SNS path exposes no explicit credential fields (only `sns_region: str`) — relying entirely on boto3's ambient credential chain with no `SecretStr` protection in the pydantic model. A host that injects `AWS_SECRET_ACCESS_KEY` via environment gets no audit trail or mask protection from the config layer.

**Remediation:** Add `aws_access_key_id: str | None = None` and `aws_secret_access_key: SecretStr | None = None` to `SmsConfig`; pass them to the SNS client when provided, falling back to ambient credentials otherwise. Mirror the pattern `EmailConfig` uses for `ses_access_key_id`/`ses_secret_access_key`.

### 🟠 W-10 — `GoogleOAuthProvider` holds `client_secret` as plain `str` (day 8 — **1 day past SLA**)

`src/regstack/oauth/providers/google.py:79,90`: `__init__` accepts `client_secret: str` and stores it as `self._client_secret = client_secret`. The pydantic config layer correctly stores the value as `SecretStr` (`OAuthConfig.google_client_secret: SecretStr | None`), but it is unwrapped before being passed to the provider constructor (in `src/regstack/app.py`). If the provider object is ever serialised or printed for debugging, the secret appears in plaintext.

**Remediation:** Change the parameter type to `SecretStr`, store as-is, and call `.get_secret_value()` only at the token-exchange POST.

### 🟢 CLEAN — Google JWKS verification

`GoogleProvider.verify_id_token` validates signature via `PyJWKClient` against Google's JWKS URL (defaulting to `https://www.googleapis.com/oauth2/v3/certs`), validates `iss`, `aud`, `exp`, `iat`, `nonce`, and `sub`. JWKS fetch is offloaded to `asyncio.to_thread` with `timeout=JWKS_FETCH_TIMEOUT_SECONDS (5)`. Algorithm pinned to `RS256`. On any failure, `OAuthIdTokenError` is raised with a generic outer message; the specific failure is logged at WARNING only, never echoed to the redirect.

---

## Section 12 — GitHub Actions Workflow Security

### 🟢 CLEAN — Third-party action pinning

All `uses:` directives in both `test.yml` and `publish.yml` are pinned to commit SHAs:
- `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd` (v6.0.2)
- `astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b` (v8.1.0)
- `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (v7.0.1)
- `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1)
- `pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b` (v1.14.0)

### 🟢 CLEAN — Untrusted input, permissions, `pull_request_target`, self-hosted runners

No `${{ github.event.* }}` interpolations inside `run:` blocks. Workflow-level `permissions: { contents: read }` on both workflows. Publish job uses `permissions: { id-token: write, contents: read }` only. No `pull_request_target` trigger in either workflow. Both use `runs-on: ubuntu-latest`.

### 🟢 CLEAN — Secrets exposure and deploy authorisation

Publish workflow uses OIDC trusted publishing (no `PYPI_API_TOKEN`). `attestations: true` generates PEP 740 sigstore provenance. Trigger is `on: { push: { tags: ["v*"] } }` only (no `workflow_dispatch`). Version verification step reads from `pyproject.toml` via `tomllib`, not by importing `regstack.version`.

### 🟢 CLEAN — Cache poisoning

`astral-sh/setup-uv` cache is keyed on `uv.lock` content hash (`cache-dependency-glob: "uv.lock"`), not user-controlled input.

---

## Section 13 — PyPI Package Integrity

### 🟢 CLEAN — Wheel and sdist contents

`[tool.hatch.build.targets.wheel] packages = ["src/regstack"]`. The `[tool.hatch.build.targets.sdist] exclude` list covers `tests/`, `docs/`, `examples/`, `.github/`, `CLAUDE.md`, `tasks/`, `tasks.py`, `scripts/`, `uv.lock`, `.git`, and `.claude/`. No developer home paths or test fixtures appear in `src/regstack/`. The `.git`-as-worktree-file edge case is explicitly excluded.

### 🟢 CLEAN — Dependency pinning, CLI entry point, publication path

All base `dependencies` entries carry annotated lower bounds corresponding to specific CVE remediation commits. Optional-only packages (`pymongo`, `asyncpg`, `aioboto3`, `twilio`, `pyjwt[crypto]`) are in `optional-dependencies`, not base `dependencies`. CLI `regstack` entry point maps to `regstack.cli.__main__:main`; subcommands do not default to any production database or hostname. OIDC trusted publishing with PEP 740 sigstore attestations is confirmed in use.

---

## Section 14 — OAuth Subsystem Audit

### 🟢 CLEAN — PKCE invariants

`_begin_flow` in `src/regstack/routers/oauth.py` generates `code_verifier = secrets.token_urlsafe(64)` server-side and stores it only on the `OAuthState` row. Only the SHA-256 challenge (`code_challenge`) is passed to the provider as a query parameter. The verifier never appears in any URL the browser sees.

### 🟢 CLEAN — State row lifecycle

State row existence, provider match, and `expires_at` are validated against `rs.clock.now()` (not `datetime.now(UTC)`) before the code exchange proceeds. After the callback succeeds, `oauth_states.set_result_token` populates the `result_token` slot; `exchange` calls `oauth_states.consume` which reads and deletes the row. Mongo uses `find_one_and_delete` (atomic). SQL non-atomicity is tracked as W-6.

### 🟢 CLEAN — ID-token verification

`GoogleProvider.verify_id_token` validates: signature via `PyJWKClient` (JWKS fetched with 5-second timeout); `iss` matches configured issuer; `aud` matches configured `client_id`; `exp > now` (enforced by pyjwt); `iat` present; `nonce` claim explicitly checked against `expected_nonce` after decode. Any failure raises `OAuthIdTokenError` with a generic outer message.

### 🟠 W-4 — Race condition: new OAuth user creation unhandled 500 (day 26 — **19 days past SLA**)

`src/regstack/routers/oauth.py:716`: in `_resolve_user`, the branch for brand-new accounts calls `await rs.users.create(new_user)` without catching `UserAlreadyExistsError`. Two concurrent requests with the same Google `sub` arriving within milliseconds can both reach this line; the second raises an unhandled exception that propagates as a 500.

**Remediation:** Wrap `rs.users.create(new_user)` in a `try/except UserAlreadyExistsError` and fall back to `rs.users.get_by_email(info.email)` to retrieve the just-created user.

### 🟢 CLEAN — Linking-policy enforcement, OAuth-only user side effects, identity-row uniqueness, hooks

`_resolve_user` raises `_LinkConflictError("email_in_use")` for existing email-registered users (when auto-link is off), `_LinkConflictError("identity_in_use")` for cross-user identity conflicts, and `_LinkConflictError("already_linked")` for same-user duplicate links. `change-password`, `change-email`, `delete-account` all call `require_password_set(user)`. OAuth unlink refuses when the user has no password and only one linked provider. `(provider, subject_id)` and `(user_id, provider)` uniqueness enforced at the DB level (Mongo unique indexes + SQL `UniqueConstraint`). All four OAuth hook events are in `KNOWN_EVENTS` and fired at the correct points.

---

## Section 15 — Multi-Backend Parity & Schema Migration Safety

### 🟢 CLEAN — CI matrix

`.github/workflows/test.yml` runs `uv sync --extra dev` which installs all four backend drivers (pymongo, asyncpg, aiosqlite, SecantusDB), then runs `pytest -n auto`. The matrix covers Python 3.11 and 3.12, with Mongo 7 and Postgres 16 as service containers. All four backends — SQLite, Postgres, MongoDB, SecantusDB — are exercised.

### 🟢 CLEAN — SQL migration safety

All three migrations (`0001_initial_schema.py`, `0002_oauth.py`, `0003_oauth_state_result_was_new.py`) define both `upgrade()` and `downgrade()`. `0002_oauth.py` uses `batch_alter_table` for the `users.hashed_password` nullable change (SQLite compatibility). The `downgrade()` guards against NULL rows before attempting NOT NULL re-enforcement.

### 🟢 CLEAN — Mongo index parity and datetime tz-awareness

`src/regstack/backends/mongo/indexes.py` declares: `users.email` unique index, `oauth_identities (provider, subject_id)` unique, `oauth_identities (user_id, provider)` unique — matching all `UniqueConstraint` entries in `src/regstack/backends/sql/schema.py`. Mongo client constructed with `tz_aware=True`. `UtcDateTime` TypeDecorator re-attaches UTC on result values. Same `Clock` instance passed to both `JwtCodec` and `UserRepo`.

---

## Open Finding Tracker

| ID | Severity | Summary | First flagged | Days open | SLA date | Status |
|----|----------|---------|---------------|-----------|----------|--------|
| W-1 | 🟠 WARNING | `cryptography` floor <50.0.1 (5 CVEs) | 2026-08-10 | 47 | 2026-08-25 (**32d overdue**) | Open |
| W-2 | 🟠 WARNING | Email-change `jti` never blacklisted | 2026-08-31 | 26 | 2026-09-07 (**19d overdue**) | Open |
| W-3 | 🟠 WARNING | Email-change handler skips bulk-revocation | 2026-08-31 | 26 | 2026-09-07 (**19d overdue**) | Open |
| W-4 | 🟠 WARNING | OAuth new-user race → unhandled 500 | 2026-08-31 | 26 | 2026-09-07 (**19d overdue**) | Open |
| W-5 | 🟠 WARNING | `aiosmtplib` floor <5.1.2 (CVE-2026-0010) | 2026-09-06 | 20 | 2026-09-13 (**13d overdue**) | Open |
| W-6 | 🟠 WARNING | SQL `OAuthStateRepo.consume` non-atomic | 2026-09-08 | 18 | 2026-09-15 (**11d overdue**) | Open |
| W-8 | 🟠 WARNING | Timing oracle on login (OAuth-only accounts) | 2026-09-18 | 8 | 2026-09-25 (**1d overdue**) | Open |
| W-9 | 🟠 WARNING | SNS credentials not `SecretStr` in `SmsConfig` | 2026-09-18 | 8 | 2026-09-25 (**1d overdue**) | Open |
| W-10 | 🟠 WARNING | `GoogleOAuthProvider` holds `client_secret` as `str` | 2026-09-18 | 8 | 2026-09-25 (**1d overdue**) | Open |
| I-1 | 🟡 INFO | `tokens_invalidated_after` in `UserPublic` | 2026-08-10 | 47 | — | Open |
| I-3 | 🟡 INFO | `UserPublic` schema includes internal field | 2026-08-10 | 47 | — | Open |

---

## 📊 Summary

- **Date:** 2026-09-26
- **Version reviewed:** 0.9.3
- **HEAD:** `c3ae87da6e4b9e5328b800f83d8652997bf552c6`
- **No product-code changes since:** 2026-09-09 (17 days)
- **CRITICAL:** 0
- **WARNING:** 9 (0 new, 9 carry-forward, 0 resolved)
- **INFO:** 2 (0 new, 2 carry-forward, 0 resolved)
- **vs. 2026-09-25:** No change in finding counts. Days-open incremented by 1 for all findings.

### Escalation note

**All nine WARNINGs are now past their 7-day SLA.** W-1 through W-6 have been overdue for 11–32 days; W-8, W-9, and W-10 crossed their SLA yesterday (1 day overdue each). There has been no product-code commit in 17 days. The rate of remediation is zero. GitHub Issues #X through #Y (opened by prior security reviews) remain unassigned. Recommend immediate escalation with hard deadline.

**Top 3 priorities for the development team:**

1. **W-1 (`cryptography` CVE floor — now 50.0.1):** Five CVEs require the bump, including a Bleichenbacher oracle (CVE-2026-69247). Single two-line change in `pyproject.toml` + `uv lock`. The OAuth JWKS + RSA paths are directly on the affected code path. Oracle Linux security advisories confirm active distribution of patches this month.
2. **W-2 + W-3 (email-change token):** Two lines of code each — blacklist the `jti` and add an `is_payload_bulk_revoked` guard. Both 19 days past SLA. Together these allow replay of email-change links after a password change.
3. **W-8 (timing oracle — OAuth-only login):** Add a constant-time dummy Argon2 call when `hashed_password is None`. One line of code. Plugs an account enumeration vector 8 days without a fix.

---

*Generated by automated daily security scan.*
