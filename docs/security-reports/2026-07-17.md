# Security Review — 2026-07-17

**Reviewer:** Automated security agent (scheduled daily review)
**Version reviewed:** 0.8.4 (`src/regstack/version.py`)
**Scope:** Full 15-section review per `scripts/security-review-prompt.md`
**Previous review:** 2026-07-16 (5 warnings, 11 INFO, 0 critical)

> **Since the last review (2026-07-16):** One commit landed on `main` —
> `[security-warning] Security Review — 2026-07-16` (the report itself).
> No product code changes. All five warnings remain open and unresolved;
> they are now **21–24 days old.** The eleven INFO items from the
> 2026-07-16 review likewise carry forward unchanged.

---

## 🔴 CRITICAL (Immediate action required)

None.

---

## 🟠 WARNING (Address within 1 week)

All five warnings carry forward from 2026-07-16. The entire remediation
queue is **four one-line `pyproject.toml` floor bumps plus `uv lock`** —
well under an hour of engineering time. These have now been open for
**21–24 days** without remediation. The age of these findings is itself
a process-health concern.

### W-1 — Four CVEs in `python-multipart <0.0.32` (DoS + smuggling cluster) — carry-forward (day 24)

**Package:** `python-multipart`
**Floor:** `>=0.0.27` · **Locked:** `0.0.28` · **Safe floor:** `>=0.0.32`
**Tracked:** GitHub issues #118 (open — duplicate of #120), #120 (open)

The locked `0.0.28` is vulnerable to all four CVEs below:

| CVE | CVSS | Fixed in | Description |
|---|---|---|---|
| CVE-2026-53537 | 3.1 Low | 0.0.30 | RFC 2231/5987 Content-Disposition decoding mismatch — header smuggling. |
| CVE-2026-53538 | 3.7 Low | 0.0.30 | `;` treated as field separator (WAF mismatch) — parameter smuggling. |
| CVE-2026-53539 | **7.5 High** | 0.0.30 | `QuerystringParser` O(B²) algorithm with `;` separators — unauthenticated CPU DoS. |
| CVE-2026-53540 | 3.7 Low | 0.0.31 | Negative `Content-Length` turns bounded read into read-until-EOF — memory DoS. |

CVE-2026-53539 (CVSS 7.5) is directly reachable via any form-encoded
endpoint in a regstack host (login, register). The floor `>=0.0.27`
permits consumers to resolve any vulnerable version.

**Action required:**
1. Bump `"python-multipart>=0.0.32"` in `pyproject.toml`. Update the
   inline CVE comment to enumerate all four CVEs.
2. Run `uv lock`.
3. Close issue #118 as duplicate of #120; update #120 to enumerate all
   four CVEs and record `>=0.0.32` as the safe floor.

---

### W-2 — Starlette floor gap covers four CVEs — carry-forward (day 23)

**Package:** `starlette`
**Floor:** `>=1.0.1` · **Locked:** `1.3.1` (safe) · **Safe floor:** `>=1.3.1`
**Tracked:** GitHub issue #121

The lock resolves `1.3.1` (fully patched). The danger is downstream:
`>=1.0.1` allows any consumer to resolve `1.0.1`–`1.3.0`:

| CVE | CVSS | Fixed in | Description |
|---|---|---|---|
| CVE-2026-48710 | 6.5–7.0 | 1.0.1 | "BadHost" — malformed `Host` bypasses path-based auth. |
| CVE-2026-48818 | **7.5 High** | 1.1.0 | Windows: `StaticFiles` UNC path leaks NTLMv2 hash. |
| CVE-2026-54282 | 3.7 Low | 1.3.0 | `request.url.hostname` attacker-controlled via non-`/` path. |
| CVE-2026-54283 | **High** | 1.3.1 | `x-www-form-urlencoded` ignores `max_fields`/`max_part_size` — DoS. |

**Action required:**
1. Bump `"starlette>=1.3.1"` in `pyproject.toml`. Update the inline
   comment to enumerate all four CVEs and cite `1.3.1` as safe floor.
2. Run `uv lock` (likely a no-op; lock already resolves `1.3.1`).
3. Update issue #121 to enumerate all four CVEs.

---

### W-3 — CVE-2026-45447: `cryptography` bundled OpenSSL heap UAF (CVSS 7.5) — carry-forward (day 21)

**Package:** `cryptography` (in `oauth` optional extra)
**Floor:** `>=46.0.7` · **Locked:** `47.0.0` (vulnerable) · **Safe:** `>=48.0.1`
**Advisory:** GHSA-537c-gmf6-5ccf · Published: June 9, 2026
**Tracked:** GitHub issue #123

All `cryptography` wheels `>=0.5.0,<48.0.1` bundle statically-linked
OpenSSL vulnerable to a heap use-after-free in `PKCS7_verify()` triggered
by an empty `digestAlgorithms` SET in a crafted PKCS#7/S/MIME message.
The locked `47.0.0` is in the affected range. Hosts installing
`regstack[oauth]` that also use `cryptography` for TLS/S/MIME/PKCS#7
operations are directly exposed.

**Action required:**
1. Bump `oauth = ["pyjwt[crypto]>=2.13.0", "cryptography>=48.0.1"]`.
   Update the inline CVE comment to add CVE-2026-45447.
2. Also bump `"cryptography>=48.0.1"` in the `dev` extra.
3. Run `uv lock`.

---

### W-4 — CVE-2026-53533: `aiosmtplib` SMTP command injection (CVSS 6.5) — carry-forward (day 21)

**Package:** `aiosmtplib`
**Floor:** `>=3.0` · **Locked:** `5.1.0` (vulnerable) · **Safe:** `>=5.1.1`
**Tracked:** GitHub issue #124

The locked `5.1.0` is the vulnerable version. `SMTP.mail()`, `SMTP.rcpt()`,
`SMTP.vrfy()`, and `SMTP.expn()` pass caller-supplied email addresses to
the SMTP server without rejecting C0 control characters (0x00–0x1F
including CR, LF, NUL). An email address containing a CRLF sequence can
inject arbitrary SMTP commands into the session at the wire level — below
Jinja2 template rendering, which cannot protect against wire-level injection.

**regstack exposure:** The SMTP backend sends verification, password-reset,
MFA, and email-change emails to user-supplied addresses. Practical risk is
reduced by `EmailStr` validation (which rejects literal CR/LF), but the
dependency floor must still be corrected.

**Action required:**
1. Bump `"aiosmtplib>=5.1.1"` in `pyproject.toml`. Update the inline
   comment to add CVE-2026-53533 and cite `5.1.1` as safe floor.
2. Run `uv lock`.

---

### W-5 — Dual CVEs in `pydantic-settings <2.14.2` (path traversal + symlink following) — carry-forward (day 21)

**Package:** `pydantic-settings`
**Floor:** `>=2.2` · **Locked:** `2.14.0` (vulnerable) · **Safe:** `>=2.14.2`
**Tracked:** GitHub issues #125 (CVE-2026-32690), #135 (CVE-2026-58203)

| CVE | CVSS | Class | Description |
|---|---|---|---|
| CVE-2026-32690 | 5.9 Medium | CWE-22 | `validate_secrets_path()` size-check skips symlinked dirs; `glob.iglob()` follows them — bypasses `secrets_dir_max_size`. |
| CVE-2026-58203 | 5.3 Medium | CWE-59 | `NestedSecretsSettingsSource` follows symlinks outside the configured secrets tree. |

`RegStackConfig` extends `BaseSettings`; deployments pointing the secrets
source at a directory where an attacker can place symlinks are at risk.

**Action required:**
1. Bump `"pydantic-settings>=2.14.2"` in `pyproject.toml`.
2. Run `uv lock`.
3. Close issues #125 and #135 together once the bump lands.

---

## 🟡 INFO (Best practice recommendations)

All eleven INFO items carry forward from 2026-07-16 unchanged. They are
reproduced here verbatim so the archive is self-contained.

### I-1 (carry-forward) — `UserPublic` exposes `tokens_invalidated_after`

`UserPublic` includes `tokens_invalidated_after: datetime | None`. This is
an intentional design choice (SPAs compare it to the session JWT `iat` to
detect bulk-revocation without an extra round-trip). The exposure leaks the
exact timestamp of the most recent security event (password change, email
change, admin-disable) to any authenticated client. Acceptable for regstack
defaults; hosts with stricter data-minimisation requirements should subclass
`UserPublic` or project out the field. No code change required in regstack
itself; document the trade-off explicitly in `docs/security.md`.

### I-2 (carry-forward) — `admin_promote_pending` path parameter not validated as `EmailStr`

`POST /admin/pending/{email}/promote` (`src/regstack/routers/admin.py:204`)
accepts `email: str = Path(...)`. A syntactically invalid email address
passes parameter binding and returns 404. The endpoint is admin-gated so
risk is low. One-line fix: `email: Annotated[EmailStr, Path(...)]`.

### I-3 (carry-forward) — Duplicate tracking issue for `python-multipart`

Issues #118 (2026-06-23) and #120 (2026-06-24) both track the
`python-multipart` floor bump. Close #118 as duplicate of #120; update #120
to enumerate all four CVEs and record `>=0.0.32` as the safe floor.

### I-4 (carry-forward) — CVE-2026-48524 missing from pyjwt comment in `pyproject.toml`

`PyJWKClient.get_signing_key()` causes a fresh JWKS fetch on every cache
miss, which can exhaust connection pools under load. Fixed at `>=2.13.0`
(current lock and floor). Add `CVE-2026-48524` alongside the existing
CVE-2026-48522/48523/48525/48526 entries during the next `pyproject.toml`
edit.

### I-5 (carry-forward) — Email-change token JTI is not blacklisted on first use

**File:** `src/regstack/routers/account.py:175`

`POST /confirm-email-change` does not blacklist the token's `jti` after
successfully completing the email swap. The `reset-password` handler does
blacklist its token's JTI at `src/regstack/routers/password.py:117`. The
email-change token has a 1-hour TTL (default). An attacker who captures
the URL from browser history, proxy logs, or email forwarding can replay
it within that window; `update_email()` accepts the second call silently
(same user, same email — no constraint violation) and bumps
`tokens_invalidated_after`, forcing the user to re-authenticate.

The practical impact is low: no email hijacking is possible (the target
address is already set), but the attacker can cause repeated forced
re-logins until token expiry. The fix is a one-liner matching the
reset-password pattern: call `await rs.blacklist.revoke(jti, exp)` after
`update_email()` succeeds.

### I-6 (carry-forward) — "X attempt(s) remaining" count in 401 response body

**File:** `src/regstack/routers/login.py:35–43`

`_build_invalid()` includes the remaining failure count in the 401 detail
message: `f"Invalid email or password. {remaining} attempt(s) remaining."`.
This tells a credential-stuffing attacker exactly when to pause to avoid
lockout, enabling systematic pacing of brute-force attempts to stay under
the threshold indefinitely. Recommendation: return a static `"Invalid email
or password."` message regardless of remaining attempts. The lockout
behaviour is unchanged; only the count disclosure is removed.

### I-7 (carry-forward) — `jwt_audience` defaults to `None`; no multi-instance deployment guidance

**File:** `src/regstack/config/schema.py`

With `jwt_audience = None`, pyjwt skips audience validation on decode. If
two regstack instances share the same `jwt_secret` (a microservice topology
where one JWT secret is shared across services), a session token issued by
instance A would be accepted by instance B. No audience guidance appears in
`docs/configuration.md` or the examples. Recommendation: add a configuration
note advising hosts to set `jwt_audience` to a service-unique string in any
deployment where multiple regstack instances share a `jwt_secret`.

### I-8 (carry-forward) — No HTTP security headers set or demonstrated

**Scope:** SSR pages served by `ui_router`

regstack does not set `Content-Security-Policy`, `X-Frame-Options`,
`X-Content-Type-Options`, or `Referrer-Policy` headers. This is expected
for an embedded library — host middleware owns the response headers. However,
neither the `examples/` demos nor `docs/embedding.md` show a recommended
middleware stack for hosts that enable `ui_router`. The absence of guidance
means hosts may ship regstack's SSR pages without appropriate defensive
headers. Recommendation: add a "Security hardening" section to the embedding
guide showing `starlette.middleware.trustedhost.TrustedHostMiddleware` and a
simple security-headers middleware configured for the regstack SSR prefix.

### I-9 (carry-forward) — `twilio_account_sid` typed as `str | None` rather than `SecretStr`

**File:** `src/regstack/config/schema.py:98`

Every other credential field in `RegStackConfig` is `SecretStr | None`:
`smtp_password`, `ses_access_key_id`, `ses_secret_access_key`,
`twilio_auth_token`, `google_client_secret`. The Twilio Account SID
(`AC…`) is a semi-public identifier that appears in Twilio URLs and console
output, but it is still a credential. Typing it as plain `str` means it
will appear unmasked in `repr()` output, Pydantic model dumps, and exception
tracebacks. One-line fix: change to `SecretStr | None` and update the access
site to `.get_secret_value()`.

### I-10 (carry-forward) — `GoogleProvider._client_secret` held as plain `str` in memory

**File:** `src/regstack/oauth/providers/google.py:88`

`GoogleProvider.__init__` receives `client_secret: str` and stores it as
`self._client_secret: str`. Once extracted from `SecretStr` at construction
in `app.py`, there is no in-memory masking. A crash dump or `__repr__` of
the provider object would include the secret in plain text. There is no
exploitable vector here given the current codebase, but adding a
`__repr__` override returning `GoogleProvider(client_id=..., client_secret=***)`
provides defense-in-depth against accidental log exposure.

### I-11 (carry-forward) — Lockout is per-email only; no per-IP protection

**File:** `src/regstack/auth/lockout.py`

`lockout.record_failure(email, ip=None)` — the `ip` argument is always
`None` in the login router (request IP is never sourced). An attacker with
a list of target email addresses can distribute guesses across accounts,
advancing each account's lockout counter slowly while making effectively
unlimited aggregate attempts. For deployments expecting credential-stuffing
attacks, per-IP rate limiting at the reverse-proxy layer is the standard
mitigation. Recommendation: document this gap in `docs/security.md` and
add a configuration guidance note in `docs/embedding.md` recommending
proxy-layer IP rate limiting (e.g., nginx `limit_req_zone`, Cloudflare
Rate Limiting) as a complement to the per-account lockout.

---

## 🟢 CLEAN (Passed review)

### §1 — Dependency CVE scan (in-lock packages)

CVE search conducted against all tracked packages for advisories published
after 2026-07-14. No new CVEs discovered for any dependency beyond those
already tracked in W-1 through W-5.

**Base packages (no new CVEs):** `fastapi 0.136.1` (CVE-2025-62727 and
CVE-2026-48710 fixed in locked version), `pydantic 2.x`, `sqlalchemy 2.0.49`,
`alembic 1.18.4`, `aiosqlite 0.22.1`, `asyncpg 0.31.0`, `pymongo 4.17.0`,
`pwdlib[argon2] 0.3.0`, `httpx 0.28.1`, `dnspython 2.8.0`,
`aioboto3 15.5.0`, `twilio 9.10.5`, `uvicorn`, `slowapi`, `click`,
`aiofiles`.

**pyjwt 2.13.0** (= lock = floor = latest): all six CVEs from June 2026
(CVE-2026-32597, CVE-2026-48522, CVE-2026-48523, CVE-2026-48524,
CVE-2026-48525, CVE-2026-48526) addressed. No new advisories found.

**jinja2 3.1.6+**: CVE-2025-27516 sandbox breakout fixed. No new advisories.

**cryptography 47.0.0**: CVE-2026-26007 (ECC private-key leak),
CVE-2026-34073 (DNS constraint bypass), CVE-2026-39892 (buffer overflow)
all fixed. CVE-2026-45447 heap UAF still tracked in W-3 (requires `>=48.0.1`).

**python-multipart 0.0.28**: CVE-2026-24486 and CVE-2026-40347 fixed.
Four-CVE cluster requiring `>=0.0.32` tracked in W-1.

**aiosmtplib 5.1.0**: CVE-2026-53533 requires `>=5.1.1` — tracked in W-4.

**pydantic-settings 2.14.0**: CVE-2026-32690 and CVE-2026-58203 require
`>=2.14.2` — tracked in W-5.

**starlette 1.3.1** (lock safe): floor gap tracked in W-2.

### §2 — Hardcoded secrets

No hardcoded secrets, credentials, private keys, or JWT bearer tokens
found in `src/`, `tests/`, `examples/`, or `docs/`. `git ls-files` returns
only `src/regstack/config/secrets.py` (the key-derivation helper — not
actual secrets). AKIA pattern hits in tests are synthetic fixtures
(`"AKIAEXAMPLE"`). `.gitignore` covers `.env`, `.env.*`,
`regstack.secrets.env`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.jks`,
`*.crt`. `git log --diff-filter=A` on sensitive filename patterns returned
only the historical `feat: explicit SES credential fields on EmailConfig
(#69)` commit, which added `SecretStr` credential fields to `EmailConfig` —
not actual credential material.

### §3 — Authentication & Authorization

- All authenticated endpoints inject `current_user()` or `current_admin()`.
- All mutating endpoints require authentication.
- Login lockout fires before user lookup and password verification — no
  timing oracle. Verified in `src/regstack/routers/login.py:73`.
- `record_failure()` wired on all four failure paths (user-not-found,
  wrong-password, inactive, unverified).
- Password reset calls `update_password` + `lockout.clear` + JTI blacklist
  (`src/regstack/routers/password.py:114–117`).
- Password change bumps `tokens_invalidated_after` via `update_password`.
- Email-change confirm (`/confirm-email-change`) bumps
  `tokens_invalidated_after` via `users.update_email`. JTI replay gap
  tracked in I-5.
- Admin `PATCH /admin/users/{id}` with `is_active=false` calls
  `set_tokens_invalidated_after` (`src/regstack/routers/admin.py:111`).
- Account deletion cascades `oauth_identities` before deleting the user
  (`src/regstack/routers/account.py:229–231`). Admin-triggered deletion
  follows the same pattern (`src/regstack/routers/admin.py:134`).
- OAuth-only users (`hashed_password=None`) return the same generic 401
  as a wrong-password attempt — no account enumeration via login.
- Per-purpose JWT key derivation confirmed: `derive_secret(master, purpose)`
  in `src/regstack/config/secrets.py`.
- Constant-time password verification via `pwdlib`.

### §4 — Storage-layer injection

All SQL queries use SQLAlchemy Core with bound parameters. The two `text()`
usages are a health-check literal (`text("SELECT 1")` in `backend.py`)
and a static migration assertion — neither accept user input. No f-string
or `%`-interpolation into raw SQL anywhere. All MongoDB queries use
structured dict filter syntax. `ObjectId.is_valid()` guards all
user-ID-to-ObjectId conversions in
`src/regstack/backends/mongo/repositories/user_repo.py`. No `$where`,
`$expr`, `$function`, or `$accumulator` operators anywhere. `UtcDateTime`
TypeDecorator in `src/regstack/backends/sql/types.py` re-attaches UTC
tzinfo on read. All SQL repos implement `purge_expired()`. `$in` query
arrays are never user-supplied.

### §5 — Input validation

`EmailStr` applied on all email-accepting models. `is_valid_e164()` called
before storing phone numbers (`src/regstack/routers/phone.py:61`). Jinja2
autoescape enabled in all template environments. Zero `| safe` filters in
`ui/templates/`. Zero `{% autoescape false %}` blocks. Field length bounds
on all user-facing string fields. Open-redirect protection in
`_validate_redirect()` rejects backslash, protocol-relative, and off-origin
URLs with a 400 (`src/regstack/routers/oauth.py:515–552`).

### §6 — Rate limiting

Per-route IP-based rate limiting is opt-in via `slowapi` config keys
(`login_rate_limit`, `register_rate_limit`, `forgot_password_rate_limit`,
etc. — all default `None`). MFA code brute-force bounded by `max_attempts`
with attempt tracking and row deletion on success. OAuth state rows have a
5-minute TTL (`state_ttl_seconds=300`) with a Mongo TTL index. No
file-upload endpoints exist. Per-IP gap documented in I-11.
Admin `/users` listing has a hard cap at `limit: int = Query(ge=1, le=200)`.

### §7 — JWT & session security

- `iat` emitted as a float (`self.iat.timestamp()` at `jwt.py:69`) —
  microsecond-precision bulk-revoke comparison correct.
- pyjwt `exp`/`iat` checks disabled (`"verify_exp": False, "verify_iat":
  False`); all expiry validation uses the injected `Clock`.
- Algorithm restricted to `Literal["HS256", "HS384", "HS512"]` — `none`
  and asymmetric algorithms excluded by design.
- Required claims enforced: `["sub", "exp", "iat", "jti", "purpose"]`.
- `_authenticate` checks: creds present → decode → `is_revoked(jti)` →
  `get_by_id(sub)` is active → `is_payload_bulk_revoked(payload, cutoff)`.
- Token decode errors return a static `"Invalid or expired token."` — no
  pyjwt detail forwarded to callers.
- No token values in log statements.
- `jti` present in every token (`secrets.token_urlsafe(16)` at encode time).

### §8 — CSP & SSR layer

Zero `style=` attributes, zero `<style>` blocks, zero inline `<script>`
tags in `src/regstack/ui/templates/`. All JS loaded via `<script src defer>`.
`oauth_complete.html` follows the same discipline. No user-supplied data
rendered server-side into HTML — `regstack.js` fills fields via API calls.
SSR templates are stateless (no `{{ user.email }}` or similar rendered
server-side).

### §9 — Data exposure

`UserPublic.from_user()` never includes `hashed_password`. No router
returns a raw `BaseUser`. No log or error-response path echoes
`jwt_secret`, `client_secret`, or `smtp_password`. Exception handling in
all routers returns generic messages. pyjwt exception detail never
forwarded to clients. OAuth callback failures redirect to
`/login?error=<short-code>` only. OAuth token-exchange and ID-token
failures are logged at `WARNING` level without echoing secrets; only a
generic error code reaches the client.

### §10 — Git history

`git log --oneline -30` reviewed. The most recent commit is the
2026-07-16 security review report. No suspicious commits to `auth/`,
`config/secrets.py`, routers, or JWT codec in the last 30 commits.
`git log --diff-filter=A` on `*.env`, `*.key`, `*.pem`, `*secret*`,
`*credential*` returned only the historical
`feat: explicit SES credential fields (#69)` — no credential material.

### §11 — Third-party integrations

- **SMTP:** `smtp_password` stored as `SecretStr`; password extracted via
  `.get_secret_value()` immediately before `aiosmtplib.send()`. Not logged.
  CVE-2026-53533 (wire-level injection) tracked in W-4.
- **SES:** `ses_access_key_id` and `ses_secret_access_key` are `SecretStr`.
  AWS credentials default to boto3 env-var chain; explicit creds validated
  as a pair. Credentials not logged.
- **SNS/Twilio:** `twilio_auth_token` is `SecretStr`. `twilio_account_sid`
  is plain `str` — tracked in I-9. Lazy imports confirmed (no base-install
  cost).
- **Google OAuth:** `google_client_secret` is `SecretStr` in `OAuthConfig`.
  Extracted to plain `str` at `GoogleProvider` construction — tracked in
  I-10. JWKS timeout set to 5 seconds (`JWKS_FETCH_TIMEOUT_SECONDS = 5`).
  Fetch offloaded to `asyncio.to_thread`. Not logged.

### §12 — GitHub Actions

- **Workflows present:** `publish.yml`, `test.yml`. No new workflows added.
- **SHA pinning:** all `uses:` directives reference commit SHAs, not tags.
  `actions/checkout`, `astral-sh/setup-uv`, `actions/upload-artifact`,
  `actions/download-artifact`, `pypa/gh-action-pypi-publish` all pinned.
- **No `pull_request_target` trigger.** No self-hosted runners.
- **Permissions:** workflow default `contents: read`. `publish` job adds
  `id-token: write` only. No `write-all`.
- **No untrusted input interpolated** into `run:` blocks.
- **No `actions/cache`** — no cache-poisoning surface.
- **`workflow_dispatch`** absent from `publish.yml`. `test.yml` retains it
  (safe — no secrets accessible via that trigger).
- **No `PYPI_API_TOKEN`** in any workflow. OIDC trusted publishing only.
- **Sigstore attestations:** `attestations: true` in `pypa/gh-action-pypi-publish`.

### §13 — PyPI package integrity

- **Wheel contents:** `[tool.hatch.build.targets.wheel]` includes only
  `src/regstack`. Tests, docs, examples, scripts, tasks excluded.
- **Sdist excludes:** `.git`, `.github/`, `CLAUDE.md`, `docs/`, `examples/`,
  `scripts/`, `tasks.py`, `tasks/`, `tests/`, `uv.lock` — all present in
  `[tool.hatch.build.targets.sdist] exclude`.
- **Tag-version verification:** reads from `pyproject.toml` via `tomllib`
  (not by importing the package).
- **Publication path:** OIDC trusted publishing, `id-token: write` only,
  trigger is tag push (`v*`), not branch push.
- **No localhost / test fixtures** in wheel contents.
- **No developer paths** (`/Users/`, `/home/`) in wheel contents.
- **PyPI version:** 0.8.4 matches `pyproject.toml` and `version.py`. No
  out-of-band publishes detected.

### §14 — OAuth subsystem

- **PKCE:** `code_verifier` generated server-side (`secrets.token_urlsafe(64)`)
  and stored on the `oauth_states` row. Only `code_challenge` (SHA-256) goes
  to the provider. Verified in `_begin_flow()` (`routers/oauth.py`).
- **State lifecycle:** callback checks missing/expired row; uses
  `rs.clock.now()` (not `datetime.now(UTC)`). `consume()` is atomic
  (read + delete) — a second exchange with the same id returns 404.
- **ID-token verification:** validates `sub`, `iss`, `aud`, `exp`, `iat`,
  `nonce` via pyjwt. Algorithm restricted to `["RS256"]`. Nonce checked
  explicitly after decode.
- **Linking policy:** `email_in_use`, `identity_in_use`, `already_linked`
  all return distinct error codes. `auto_link_verified_emails` defaults to
  `False`. Re-linking the same identity raises `already_linked`.
- **OAuth-only user side effects:** login, change-password, change-email,
  delete-account all correctly handle `hashed_password=None`.
- **Identity uniqueness:** `(provider, subject_id)` and `(user_id, provider)`
  both have DB-level unique indexes on Mongo (`provider_subject_unique`,
  `user_provider_unique`) and SQL (`UniqueConstraint`).
- **Hooks:** `oauth_signin_started`, `oauth_signin_completed`,
  `oauth_account_linked`, `oauth_account_unlinked` confirmed in
  `hooks/events.py` and fired at the correct points.

### §15 — Multi-backend parity & schema migration safety

- **CI matrix:** `test.yml` runs all tests against SQLite + Postgres +
  MongoDB (both Python 3.11 and 3.12). Three-backend coverage confirmed.
- **Migrations:** `0001_initial_schema.py`, `0002_oauth.py`, and
  `0003_oauth_state_result_was_new.py` all have `upgrade()` and
  `downgrade()`. `0002_oauth.py` uses `batch_alter_table` for SQLite
  compatibility.
- **Autogen-drift test:** `tests/integration/test_sql_migrations.py`
  present and verified.
- **Mongo index parity:** `users.email` UNIQUE, `oauth_identities
  (provider, subject_id)` UNIQUE, `oauth_identities (user_id, provider)`
  UNIQUE — all declared in `src/regstack/backends/mongo/indexes.py`. TTL
  indexes present on `expires_at` for `pending_registrations`,
  `token_blacklist`, `login_attempts`, `mfa_codes`, `oauth_states`.
- **Datetime tz-awareness:** `make_client(tz_aware=True)` for Mongo,
  `UtcDateTime` TypeDecorator for SQL. Both confirmed present.

---

## 📊 Summary

| | |
|---|---|
| **Date** | 2026-07-17 |
| **Version** | 0.8.4 |
| **🔴 Critical** | 0 |
| **🟠 Warning** | 5 (all carry-forward; oldest is day 24) |
| **🟡 Info** | 11 (all carry-forward) |
| **🟢 Clean sections** | 15 of 15 structurally sound |

**vs. previous review (2026-07-16):** No change in finding counts. No
product code landed. All five warnings and eleven INFO items carry forward
without resolution.

**Top 3 priorities for the development team:**

1. **W-1 / python-multipart ≥0.0.32** — CVE-2026-53539 (CVSS 7.5) is
   unauthenticated CPU DoS reachable via login/register. One-line
   `pyproject.toml` bump. Overdue by 24 days.

2. **W-2 / starlette floor ≥1.3.1** — Locked version is already safe;
   only the floor needs raising. Protects consumers who pin to any
   `starlette` version under 1.3.1 from CVE-2026-54283 (DoS) and
   CVE-2026-48818 (NTLMv2 hash leak). One-line bump. Overdue by 23 days.

3. **W-3 / cryptography ≥48.0.1** — Heap UAF in bundled OpenSSL
   (CVSS 7.5) affects all `regstack[oauth]` consumers. One-line bump in
   both `oauth` and `dev` extras. Overdue by 21 days.

> **Process note:** Five security warnings have now been open for 21–24
> days without any remediation commit. The combined fix is approximately
> four lines in `pyproject.toml` and one `uv lock` run. The pattern of
> daily reports with no action represents a process gap, not a technical
> blocker. At 24 days, W-1 (unauthenticated DoS) in particular warrants
> escalation beyond the automated review.
