# Security Review — 2026-09-04

**Reviewer:** Automated daily review (Claude Code)
**Version reviewed:** regstack 0.9.3 (`src/regstack/version.py`)
**HEAD commit:** `f253bd3` ([security-warning] Security Review — 2026-09-03)
**Previous review:** 2026-09-03 — 4 WARNING (1 CVE carry-over, 3 product-code), 12 INFO, 0 CRITICAL
**Comparison:** No code changes since yesterday (sole commit added the 2026-09-03 security report).
All four WARNING-level findings carry over unchanged. Net: 4 WARNING, 12 INFO, 0 CRITICAL.

---

## 🔴 CRITICAL

None.

---

## 🟠 WARNING

### W-1 (carry-over, day 25 — SLA MISSED 2026-08-18)

**Section:** §1 — Dependency Vulnerability Check
**CVE:** CVE-2026-69247 — `cryptography` Bleichenbacher oracle (`pkcs7_decrypt`)
**Affected range:** `cryptography` ≥ 44.0.0, < 50.0.0
**Resolved in:** `cryptography==50.0.0` (released 2026-07-31 — 35 days ago)
**Current resolved version in `uv.lock`:** 49.0.0 (within affected range)
**Current floor in `pyproject.toml`:** `cryptography>=48.0.1` (does not exclude affected versions)
**Fix required:** Bump floor to `cryptography>=50.0.0` in both the `oauth` and `dev` extras in
`pyproject.toml`; run `uv lock`.
**Tracking:** GitHub issue #181 (`[security] Raise cryptography floor to >=50.0.0`), open since
2026-08-19 — no fix PR, no assignee, no comment — **17 days unacknowledged.**

**Technical impact:** `pkcs7_decrypt_der`, `pkcs7_decrypt_pem`, and `pkcs7_decrypt_smime` expose
distinguishable error paths and timing differences during RSA `RecipientInfo.encryptedKey` decryption,
constituting a Bleichenbacher-style oracle. regstack does not call these functions directly, but
`pip install regstack[oauth]` resolves to 49.0.0 by default, shipping a version with an open advisory
to all downstream consumers. The fix (cryptography 50.0.0+) substitutes a random key on RSA
decryption failure per RFC 3218.

**Urgency:** The fix version has been on PyPI for 35 days. The 7-day SLA lapsed 2026-08-18. This
finding will appear daily until resolved.

---

### W-2 (carry-over, day 4 — NEW 2026-09-01) — `POST /confirm-email-change` email-change JWT not revoked after use

**Section:** §3 — Authentication & Authorization Audit
**File:** `src/regstack/routers/account.py`, `confirm_email_change` handler
**Status:** Unresolved. No change to `account.py` since first flagged.

The `confirm_email_change` endpoint successfully updates the user's email address but does not
call `await rs.blacklist.revoke(token_payload.jti, token_payload.exp)` after doing so. The
password-reset path (`password.py`) correctly performs this revocation after every successful
reset. Within the email-change token's TTL, the same confirmation URL can be submitted repeatedly.
Each subsequent submission attempts `update_email`, which succeeds as a no-op when the address is
already the user's own, and the endpoint returns HTTP 200 — confirming the link is still usable.

The `_decode_email_change_token` helper currently returns only `(user_id, new_email)` and discards
the `jti` and `exp` claims. Any fix must also thread these through to the caller.

**Fix:** Expand `_decode_email_change_token` to return `(user_id, new_email, jti, exp)` and add
`await rs.blacklist.revoke(jti, exp)` immediately after the successful `update_email` call.

---

### W-3 (carry-over, day 4 — NEW 2026-09-01) — `POST /confirm-email-change` bulk-revoke not applied to email-change tokens

**Section:** §3 — Authentication & Authorization Audit
**File:** `src/regstack/routers/account.py`, `confirm_email_change` handler
**Status:** Unresolved. No change to `account.py` since first flagged.

`AuthDependencies._authenticate` calls `is_payload_bulk_revoked(payload, user.tokens_invalidated_after)`
for session tokens. The `confirm_email_change` endpoint decodes the email-change JWT and calls
`get_by_id` + `update_email` with no equivalent temporal cutoff check. Because the purpose-derived
key correctly prevents session tokens from satisfying this endpoint, the purpose isolation is sound —
but a stale email-change token minted before a password-change event can still be used after
`tokens_invalidated_after` has been bumped.

**Attack scenario:**
1. User initiates email-change → token minted at `iat=T1`, link emailed.
2. User detects compromise of email account, changes password → `tokens_invalidated_after` bumped to `T2 > T1`.
3. Attacker (who has inbox access) clicks the email-change link minted at `T1` → `confirm_email_change`
   succeeds because `T1 < T2` is never evaluated.
4. Attacker now controls the user's email address.

**Fix:** After decoding the email-change JWT in `confirm_email_change`, fetch the user record and check
`if rs.auth.is_payload_bulk_revoked(token_payload, user.tokens_invalidated_after): raise HTTPException(400, ...)`.
The pattern mirrors the `_authenticate` dependency. Both W-2 and W-3 can be fixed in a single pass
through `confirm_email_change`.

---

### W-4 (carry-over, day 4 — NEW 2026-09-01) — OAuth `_resolve_user` unhandled `UserAlreadyExistsError` → HTTP 500

**Section:** §3 / §14 — Authentication & Authorization / OAuth Subsystem
**File:** `src/regstack/routers/oauth.py`, `_resolve_user` (brand-new account path, line ~716),
`oauth_callback` (line ~325)
**Status:** Unresolved. No change to `oauth.py` since first flagged.

A race between a concurrent email registration and the OAuth sign-in flow creates an unhandled
exception path that returns HTTP 500 to the user's browser. `oauth_callback` catches only
`_LinkConflictError`; `UserAlreadyExistsError` from `rs.users.create()` propagates unhandled.

**Scenario:**
1. User clicks "Sign in with Google" with `email@example.com`.
2. `_resolve_user` calls `get_by_email("email@example.com")` → returns `None`.
3. Concurrently, another request completes `POST /register` with the same email.
4. `_resolve_user` calls `rs.users.create(new_user)` → raises `UserAlreadyExistsError`.
5. `oauth_callback` catches only `_LinkConflictError`; `UserAlreadyExistsError` propagates as HTTP 500.

**Fix:** In `_resolve_user`, wrap `rs.users.create(new_user)` in a `try/except UserAlreadyExistsError`
and re-raise as `_LinkConflictError("email_in_use")`. This is consistent with how the auto-link
policy already handles the known-email case.

---

## 🟡 INFO

### I-1 — Issue #181 unacknowledged (day 17)

GitHub issue #181 (`[security] Raise cryptography floor to >=50.0.0`), opened by the 2026-08-19
automated review to track W-1, remains open with no PR, no assignee, and no comment as of today
(17 days). The fix is a two-line change in `pyproject.toml` plus `uv lock`.

### I-2 — CVE-2026-24486 (python-multipart path traversal) — already covered (carry-over)

CVE-2026-24486 affects `python-multipart < 0.0.22`. regstack's floor is `python-multipart>=0.0.32`
— well past the fix version. regstack has no user-facing file-upload endpoints. No action required;
flagged for awareness.

### I-3 — SecantusDB pre-release in `secantus` and `dev` extras (carry-over)

`SecantusDB>=0.6.0b2` is a pre-release dependency. No active CVE or instability observed. Upgrade
to a stable release when published.

### I-4 — GitHub Actions SHA pins slightly behind latest minor releases (carry-over)

All `uses:` directives are correctly SHA-pinned (no mutable tag references). The pins are slightly
behind latest minor versions: `actions/checkout@de0fac2e` (v6.0.2), `astral-sh/setup-uv@08807647`
(v8.1.0), `actions/upload-artifact@043fb46d` (v7.0.1), `actions/download-artifact@3e5f45b2`
(v8.0.1), `pypa/gh-action-pypi-publish@cef221092` (v1.14.0). No active security regression;
consider bumping on next maintenance cycle.

### I-5 — SNS backend uses ambient boto3 credential chain; no explicit credential config (carry-over)

**File:** `src/regstack/sms/sns.py`, line 29
`aioboto3.Session()` is constructed with no credentials — always falls back to boto3's ambient chain
(env vars, IAM instance profile). The `SmsConfig` model has no `sns_access_key_id` /
`sns_secret_access_key` fields, unlike the SES email backend which offers explicit key
configuration. Hosts cannot supply explicit SNS credentials via `regstack.toml`; they must rely on
ambient IAM roles. This asymmetry with the SES backend should be documented in the operator guide.

### I-6 through I-12 (carry-over from prior reviews — no change)

Findings I-6 through I-12 carried in the 2026-09-03 report (sdist locale leaks, PEP 740
attestation enabled, base-install smoketest in CI, docs-only workflow permissions, two-workflow
structure confirmed, publish OIDC-only, no self-hosted runners) remain unchanged. All confirmed
CLEAN on today's scan.

---

## 🟢 CLEAN

The following areas were re-checked today and remain secure:

- **§2 Hardcoded secrets:** No API keys, JWTs, private keys, or real credentials in `src/`, `tests/`,
  `examples/`. The two JWT fragments in `tests/` are invalid test fixtures. `.gitignore` correctly
  covers `.env`, `regstack.secrets.env`, `*.pem`, `*.key`, `*.p12`, `htmlcov/`, `.coverage`.
  `git ls-files` surfaces only `src/regstack/config/secrets.py` (the key-derivation module, not
  a credential file).

- **§3 Auth/authz (excluding W-2/W-3/W-4):** All authenticated routes inject `current_user` or
  `current_admin`. Per-token blacklist and bulk-revoke checks both present in
  `_authenticate`. JWT `iat` emitted as float (confirmed in `src/regstack/auth/jwt.py` line 69).
  pyjwt `exp`/`iat` checks disabled; `Clock`-injection seam intact. Constant-time password
  comparison via `pwdlib` (not `==`). JWT keys derived per-purpose via `derive_secret`.
  OAuth-only user (`hashed_password=None`) handled in login with same generic 401.

- **§4 Storage-layer injection:** No `$where`/`$expr`/`$function` operators in Mongo repos.
  `ObjectId.is_valid()` guard present on all `get_by_id` paths. SQL repos use SQLAlchemy Core
  parameterised binds; no f-string interpolation into raw SQL. The two `text(...)` usages
  (`SELECT 1` health check and migration count query) are non-parameterised but not user-controlled.

- **§5 Input validation:** All API inputs go through Pydantic models. `EmailStr` validated on
  email-bearing fields. E.164 validation confirmed in phone routes. Jinja2 autoescape on in email
  templates; no `| safe` or `{% autoescape false %}` in SSR templates. `redirect_to` validated
  same-origin via `_validate_redirect` (backslash and `//` prefix checks included).

- **§6 Rate limiting / DoS:** Login lockout wired via `LockoutService`. Admin listing endpoint
  caps `limit` at 200 (`ge=1, le=200`). OAuth state TTL confirmed. No user-facing file upload
  endpoints.

- **§7 Session/token security:** All tokens include `jti`. `iat` is float. Bulk-revoke uses `<=`
  cutoff. `UserPublic` excludes `hashed_password` and internal fields in all response models.

- **§8 CSP / SSR layer:** No `<style>` blocks, `style="..."` attributes, or inline `<script>` tags
  found in `src/regstack/ui/templates/`. `oauth_complete.html` follows the same discipline.
  SSR pages are stateless — no `{{ user.email }}` or auth state rendered into HTML.

- **§9 Data exposure:** Responses use `UserPublic.from_user()` throughout, including admin router.
  `hashed_password` reference at `admin.py:180` is internal to `PendingRegistration` construction,
  never in the HTTP response. Error messages: no raw exception leakage found; OAuth errors redirect
  with short codes. Secrets (`jwt_secret`, `google_client_secret`, `smtp_password`) stored as
  `SecretStr` throughout `config/schema.py`.

- **§10 Git history:** `git log --oneline -30` shows only daily security review commits since
  2026-08-06 — no changes to auth, config, routers, or the JWT codec. No accidentally committed
  secrets in git history.

- **§11 Third-party integrations:** SMTP password is `SecretStr`. SES credentials from config
  (aside from I-5 above for SNS). Lazy imports for SES/SNS/Twilio confirmed. Google OAuth verifies
  ID token via `PyJWKClient` + `pyjwt.decode`, validating `iss`, `aud`, `exp`, `nonce`.

- **§12 GitHub Actions:** Two workflows: `publish.yml`, `test.yml`. All `uses:` SHA-pinned.
  No `${{ github.event.* }}` interpolations in `run:` blocks. Workflow-level `permissions: contents: read`
  on both; `publish` job adds only `id-token: write`. No `pull_request_target` trigger. No
  self-hosted runners. No `PYPI_API_TOKEN` secret — OIDC trusted publishing only.

- **§13 PyPI package integrity:** Wheel target set to `packages = ["src/regstack"]`; sdist
  explicitly excludes `tests/`, `docs/`, `examples/`, `scripts/`, `tasks/`, `tasks.py`,
  `CLAUDE.md`, `.git`. Version tag check reads from `pyproject.toml` (not `import regstack`).
  `attestations: true` on `pypa/gh-action-pypi-publish`. base-install smoketest job in CI.
  `tests/unit/test_base_install_imports.py` guards against optional-extra leak to base install.

- **§14 OAuth subsystem:** PKCE `code_verifier` stored on state row, only `code_challenge`
  in redirect URL. State rows consumed atomically via `rs.oauth_states.consume()` — second exchange
  with same id returns 404. ID-token verification validates signature (JWKS), `iss`, `aud`, `exp`,
  `nonce`. Auto-link requires `email_verified=True`. `(provider, subject_id)` and
  `(user_id, provider)` uniqueness enforced at DB level. `oauth_signin_started/completed`,
  `oauth_account_linked/unlinked` present in `KNOWN_EVENTS`.

- **§15 Multi-backend parity:** CI matrix runs all four backends (sqlite, postgres, mongo, secantus).
  Migrations include `batch_alter_table` for SQLite column alterations. `make_client(tz_aware=True)`
  for Mongo; `UtcDateTime` TypeDecorator for SQL. Mongo index parity checked: `users.email` unique
  index, `oauth_identities (provider, subject_id)` unique, `(user_id, provider)` unique all present.

---

## 📊 Summary

- **Date of review:** 2026-09-04
- **Version reviewed:** 0.9.3
- **Total issues found by severity:** 0 CRITICAL / 4 WARNING / 12 INFO
- **Comparison:** Same findings as 2026-09-03. No code changes since yesterday.

### Top 3 priorities for the development team

1. **[W-1 / OVERDUE]** Bump `cryptography>=50.0.0` in `pyproject.toml` and re-run `uv lock`. Issue #181
   has been open 17 days with no response. This is a two-line fix shipping a PyPI advisory to every
   `pip install regstack[oauth]` consumer.

2. **[W-2 / W-3]** Fix `confirm_email_change` in `src/regstack/routers/account.py`: (a) revoke the
   email-change JWT's `jti` via `rs.blacklist.revoke` after success, and (b) check
   `is_payload_bulk_revoked` against `user.tokens_invalidated_after` before processing. Both fixes
   belong in the same PR.

3. **[W-4]** Wrap `rs.users.create(new_user)` in `_resolve_user` (`src/regstack/routers/oauth.py`,
   line ~716) in a `try/except UserAlreadyExistsError` that re-raises as `_LinkConflictError("email_in_use")`.
   This prevents HTTP 500 on a benign race condition.
